# Changelog

形式: [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) / バージョニング: SemVer

> 本ファイルはユーザー向けの主要変更を抜粋したものです。全変更のラウンド別完全記録は
> git 履歴（コミットメッセージ先頭の `Round N:`）と [docs/FEATURE_AUDIT.md](docs/FEATURE_AUDIT.md)
> （Round 464–510 の監査記録）を参照してください。

## [Unreleased]
### Added
- **表情エディタ** — 喜/怒/哀/楽の4感情を、既存モーフ10種（あ〜お・まばたき・4感情）の重み0–100のミックスとして編集可能に。未編集時は従来と完全に同一のVRMを書き出す。
- **複数段Undo/Redo** — Ctrl+Z / Ctrl+Shift+Z で最大20段。スライダー・プリセット・ガチャ・JSON読込・メタ入力すべてが対象。Redoの存在はUndo後のヒント表示で発見可能。
- **ガチャのカテゴリロック** — 体格/顔/髪/衣装/配色の5分類を個別にロックし、気に入った部分を保ったまま再ガチャできる。
- **シード共有** — `?seed=N` 付きURLで同一ガチャ結果を再現。「リンクをコピー」「シードをコピー」ボタン、シード番号の手動入力にも対応。
- **PNGスクリーンショット** — ヘッダボタンまたは Ctrl+Shift+P。モバイルでは Web Share API による共有シートに対応。
- **クリップボード連携** — パラメータJSONの「コピー」「貼り付け」ボタン（保存ファイルを介さない受け渡し）。
- **保存先を選べる書き出し** — File System Access API 対応ブラウザ（Chrome/Edge）ではVRM/JSONの保存先・ファイル名をダイアログで指定可能（非対応ブラウザは従来通りダウンロード）。
- **MToonアウトライン書き出しトグル** — 色タブ（詳細モード）でアウトラインON/OFFを選択して書き出し（PC向け。Quest非対応の注記付き）。
- **表情プレビューバー** — 3Dプレビュー上で喜怒哀楽・ニュートラルをワンタップ切替（キーボード操作・スクリーンリーダー対応）。
- **プリセット差分表示** — プリセットから変更を加えると●インジケータと「元に戻す」ボタンを表示。
- **自動保存バッジ** — localStorage への保存成功/故障を常時表示（故障時は警告に切替）。
- **キーボードショートカット拡充** — 数字1–8でタブ切替、Mでかんたん/詳細切替、?でヘルプ、Shift+↑↓でカメラ上下パン、ダブルクリック/Delete/Backspaceでスライダーをデフォルト値へリセット。ショートカット一覧はAboutダイアログに掲載。
- **統計の充実** — 揺れ物チェーン数の行、Quest Excellent閾値の注記、VRM推定ファイルサイズ表示、律速カテゴリのインライン表示。
- **モバイル対応強化** — 2本指ドラッグでカメラ上下パン、dvh単位によるレイアウト安定化、iOS Safariのフォーカス時自動ズーム防止、ホーム画面追加時のスタンドアロン動作。
### Changed
- **VRChat Performance Rank基準値を2026-04-21版へ同期** — 上流で追加されたRaycastsカテゴリを推定器テーブルに反映（雛の出力は常に0で、既定プリセットのランク保証に影響なし）。
- **WCAG 2.2 AA対応** — 新設のSC 2.5.8（ターゲットサイズ24px）に合わせカラースウォッチ・チェックボックスを24×24pxへ拡大。Windowsハイコントラストモード（forced-colors）でプリセット選択・タブ選択・表情ボタンの選択状態が判別できるようアウトラインを追加。
- **全プリセットの三角形数を112削減** — 球面プリミティブの極で常に生成されていた面積ゼロの縮退三角形を除去（見た目の変化なし）。
- **ブラウザタブ/共有タイトルの一貫化** — 言語設定とアバター名を常に両立する形式（`雛 — {名前}` / `Hina — {name}`）に統一。
### Fixed
- **クリップボード読み取りに非対応のブラウザで、貼り付けの失敗理由が誤って表示されていた問題を修正** — Firefox/Safari では「クリップボードの内容が不正です」と表示され、**利用者のデータに問題があるかのように**読めた。実際はブラウザ側が読み取りに対応していないだけなので、その旨と「パラメータ読込」でファイルから読む代替手段を案内するようにした。
- **スクリーンリーダーの読み上げが直後に別の内容で上書きされていた問題を修正** — 出力タブでは、再描画のたびに「ファイル名: …」の読み上げが0.5秒後に予約されており、「Quest Excellent にする」など**操作そのものの読み上げを打ち消していた**。ファイル名が実際に変わったときだけ読み上げるよう修正（重複抑制の判定が、再描画で作り直される要素を見ていたため一度も機能していなかった）。
- **シードを入力して Enter を押すと、そのシードではなくランダムなアバターになっていた問題を修正** — 入力欄は Enter を促す設定（スマホのキーボードでは「実行」キー表示）だったが、Enter の既定動作がガチャボタンを押してしまい、入力したシードが即座に上書きされていた。Tab で欄を離れた場合は正常だったため気付きにくかった。共有されたシードを入力する導線がこれで意図どおり動作する。
- **効果のない「服サブ」カラーピッカーを、使わない衣装では表示しないように** — サブカラーを実際に使うのは `shirts` のみで、`onepiece`（既定）・`sailor`・`hoodie` ではどの三角形もそのテクスチャ領域を参照していなかった。それでも色タブにピッカーが並んでいたため、既定の衣装で色を変えてもプレビューにも書き出したアバターにも一切変化が無かった。スカート丈（`skirtLen`）と同じ扱いにし、効果のある衣装でのみ表示する。
- **アバター名によっては書き出したファイルが見つからない/開けない問題を修正** — 名前が「.」で始まると隠しファイルになり、`CON`や`NUL`など Windows の予約名を拡張子付きで使うと保存できませんでした。あわせて、名前に含まれる制御文字や文字表示を逆転させる特殊文字（ファイル名の偽装に使われうる）も除去するようにしました。
- **読み込んだJSONの長すぎるメタ情報が書き出し時に無音で切り詰められる問題を修正** — パラメータJSONを読み込むと、名前や作者名が256文字を超えていてもそのまま画面に表示され、VRM書き出し時にだけ黙って切り詰められていました。読込時に入力欄・書き出しと同じ上限（256文字・制御文字除去）を適用して揃えました。
- **スマホで2本目の指が触れた後に視点が大きく跳ぶ不具合を修正** — 3Dプレビューを1本指で回している最中に別の指が触れて離れると、次に指を動かした瞬間に視点が最大で約86°飛んでいた。指ごとに基準位置を持つよう修正（通常の回転・ピンチズームの操作感は変わりません）。
- **プレビューでアバターの一部が暗く塗り潰されていた不具合を修正** — 輪郭線の描画が本体より手前に来てしまい、頭・胴・手・足が本来の色ではなく暗色で覆われていた（可視部分の約39%）。三角形の面の向きをglTF標準（反時計回り）に統一して解消。書き出されるVRMの形状・色・サイズは一切変わらない。
- **書き出しの整合性保証** — 書き出し処理中にパラメータや色を変更しても、書き出されるVRMはボタンを押した瞬間の状態（ジオメトリ・メタ・表情・テクスチャすべて）で固定されるように修正。以前は保存ダイアログ表示中の色変更がテクスチャにだけ反映され、形状と色が食い違うVRMが生成されうる不具合があった。
- **アウトライン書き出しが一部のビューアで表示されない不具合を修正** — 色タブでアウトラインをONにして書き出したVRMが、MToonのアウトライン用シェーダーキーワード（`MTOON_OUTLINE_WIDTH_WORLD`／`MTOON_OUTLINE_COLOR_FIXED`）を欠いていたため、キーワードでシェーダーを切り替える一部のVRMビューア（three-vrm・Babylon.js・Godot系）でアウトラインが描画されなかった。該当キーワードを付与して解消（アウトラインOFFの既定動作・形状・色は不変）。
- **glTF仕様違反の修正** — モーフターゲットaccessorのmin/maxが実データと厳密一致していなかった問題（Khronos公式バリデータがErrorとして検出する違反。全書き出しで発生していた）を修正。
- **Caps Lock有効時にCtrl+S等のショートカットが無反応になる問題を修正** — さらにブラウザネイティブの「ページを保存」ダイアログへフォールスルーしていた。
- **破損VRMのサイレントダウンロード防止** — 書き出し直前にGLBヘッダを検証し、不正なら中断して通知。
- **Windows予約デバイス名（CON等）のファイル名対策** — 該当時は自動でリネームしWindowsでの保存不能を回避。
- **連打・再入への防御** — VRM書き出し/スクリーンショット/JSON保存の全てに再入防止ガードを追加し、二重実行による「古い保存が新しい保存を上書きする」競合を防止。
- **アクセシビリティの大規模改善** — エラー通知のスクリーンリーダー読み上げ統一（assertive領域への一元化）、Aboutダイアログ表示中の背景コンテンツ完全遮断（inert）、非同期操作後のフォーカス復元、範囲外入力のaria-invalid通知、Firefox+NVDAの幽霊アナウンス対策、ほか多数。
### Security
- **prototype pollution対策** — JSON読込の全4経路（貼り付け・ファイル・ドラッグ&ドロップ・localStorage復元）で `__proto__` キーによるプロトタイプ書き換えを遮断するホワイトリスト方式のサニタイズを導入。
- **貼り付けDoS対策** — クリップボード経由のJSONにもファイル読込と同じ2MB上限を適用。
- **メタデータ/シードの値検証** — ライセンス等のenum値とガチャシードを、読込・保存・書き出しの全層で正規値へ検証（不正値の永続化を防止）。

---

以下は 0.1.0 直後（Round 11–45 期）の詳細ログ（上記の要約に統合済み。参考として保持）:

#### Added
- enum パラメータ（目の形・眉・髪型・前髪・衣装・袖）の選択肢を日英ローカライズ表示（従来は内部ID `round`/`sailor` 等を直接表示していた）。`enum.<key>.<value>` キーで管理し、テストが全選択肢のja/enラベル網羅を担保。
- 詳細モードのスライダー横に**数値直接入力欄**を追加（SPEC §2「全パラメータ＋数値入力」の充足）。範囲外入力はクランプ。
- Performance Rank の**律速項目を明示**（SPEC F-010）。PC/Quest ランクのバッジに、ランクを律速しているカテゴリ名（日英）をツールチップ＋小テキストで表示。
- 性能カテゴリ（tris/bones/pbComp 等）に日英ラベル `cat.*` を追加。
- モーフターゲットをフル配列（全頂点×3float）から **glTF sparse accessor**（差分頂点のみ）に変更（Plan.md §リスク「v0.2でsparse化」を前倒し）。既定ビルドで 193,968 B → 2,058 B（98.9% 削減）。UniVRM互換・テスト担保。
- MToon `_OutlineWidthMode` を `1`（WorldSpace ON）から `0`（OFF）に修正。SPEC §5.5「アウトラインは既定OFF（Unity側で任意有効化）」の充足。
- 出力タブの統計表に**VRM推定ファイルサイズ**を追加（`~XXX KB`形式・テクスチャ込み）。`estimate()` が `approxBytes` を返すようになり、テストで妥当範囲を担保。
- Aboutダイアログのバージョン表示を `HINA.VERSION` から動的に設定（ハードコード `v0.1.0` を排除）。
- `note.upload` テキストから存在しない `docs/UPLOAD_GUIDE.md` の参照を削除（アプリ内ガイドで代替済み）。
#### Fixed
- **ライセンスドロップダウンのローカライズ**: Exportタブのライセンス選択肢が `Redistribution_Prohibited`、`CC_BY_NC_SA` 等のraw技術値で表示されていた（`selRow` 第4引数が `null`）。`license.*` i18n キー8種（ja/en）を追加し、ユーザー向け表示（「再配布禁止」「No redistribution」等）に変更。
- **`rebuild()` エラーバウンダリ追加**: `buildAvatar()` が例外を投げた場合 `build` が null のまま機能停止していた。try/catch を追加し `err.buildFailed` i18n キーで alert 通知。テスト担保。
- **メタ入力欄に placeholder ヒント追加**: タイトル・作者フィールドに placeholder（`out.title.ph`・`out.author.ph`、ja/en）を追加。タイトル未入力のままエクスポートすると全ファイルが `hina.vrm` になる問題への入力促進。
- **i18n `note.upload` 死エントリ除去**: 両言語（ja/en）の i18n オブジェクトに、修正済みの正エントリとは別に旧エントリ（`docs/UPLOAD_GUIDE.md` を参照するテキスト）が重複キーとして残存していた。JavaScript はオブジェクトリテラルの重複キーで後勝ちのため動作上の影響はなかったが、混乱を招くコードスメルだったため削除。
- **モバイル向け meta タグ追加**: `<meta name="theme-color" content="#0F1216">` を追加し、Android Chrome 等でブラウザクロムをアプリの黒背景と統一。`<meta name="apple-mobile-web-app-capable" content="yes">` を追加し、iOS でホーム画面に追加した際に全画面スタンドアロンモードで動作するようにした。
- **`prefers-reduced-motion` 動的追従**: OS のアクセシビリティ設定変更をページリロードなしにリアルタイム反映するよう修正。`MediaQueryList.addEventListener('change', ...)` で `reduceMotion` 変数を更新し、次の `renderFrame()` から即時に反映される（呼吸・頭揺れ・瞬き・髪物理が即停止/再開）。
- **About ボタンの `aria-label` ローカライズ**: `aria-label="About"` をハードコード英語から `a11y.about.btn` i18n キー（ja: 「雛について」/ en: 「About Hina」）に変更。`applyLang()` が言語切替時に動的更新。日本語モードで「About ボタン」と英語で読まれる問題を解消。
- **`<noscript>` フォールバック追加**: JavaScript 無効時に空白ページを表示する代わりに、日英両言語で「JavaScript が必要です」と案内するメッセージを `<body>` 開始直後に追加。既存 CSS 変数（`--bg` / `--text` / `--text-dim`）を使用してデザインを統一。
- **selfTest 強化 — bone/node インデックス整合性検証追加**: selfTest() に (1)ヒューマノイドボーンの全 node インデックスが `nodes[]` 範囲内か (2)SpringBone colliderGroup の node インデックスが範囲内か、の2件を追加（18件→20件）。無効な node 参照による UniVRM 読み込み失敗を早期発見できる。
- **About ダイアログのアクセシビリティ強化**: `<dialog>` に `aria-labelledby="aboutH2"`、`<h2>` に `id="aboutH2"` を追加。スクリーンリーダーが「雛 (Hina) v0.1.0 ダイアログ」とタイトル付きで読み上げるようになった。閉じるボタンのテキストを `OK` から `about.close` i18n キー（ja: 「閉じる」/ en: 「Close」）に変更し言語切替に対応。
- **WebGL エラーメッセージの i18n 対応**: WebGL 使用不可時（`hint.noGL`）とコンテキスト消失時（`hint.glLost`）のヒントをハードコード英語から i18n キー（ja/en）に変更。`applyLang()` が言語切替時に WebGL 状態を確認して適切なメッセージを表示するよう修正（以前は無条件で `hint.drag` を設定し WebGL エラーメッセージを上書きしていた）。
- **VRM meta 入力欄に maxlength="256" 追加**: VRM ライターの `str()` が `.slice(0,256)` でサニタイズするため、ブラウザ側でも256文字上限を `maxlength` で明示。無音切り詰めを防ぎ、ユーザーが意図せず長い文字列を送信することを防止。title・author・contact・reference の全フィールドに適用。
- **ガチャシード再入力フォーム**: プリセットタブのガチャセクションにシード番号入力欄を追加。数値を入力してEnterキーを押すと同じシードでアバターを再生成でき、友人とシード番号を共有することで同一アバターを再現できる。gacha.seed.ph プレースホルダーi18nキー（ja/en）追加。ガチャボタンと再入力を `runGacha(seed)` に統一。
- **WCAG ランドマーク構造強化**: `<section id="stage">` と `<aside id="panel">` に `aria-label`（`a11y.stage`・`a11y.panel` i18n、ja/en）を追加。名前なし `<section>` はARIA ランドマークにならないため、スクリーンリーダーのランドマーク移動（NVDA: R / VoiceOver: VO+Cmd+L）で「3Dプレビューエリア」「アバター設定パネル」として到達可能になった。言語切替時は `applyLang()` で動的更新。
- **WCAG 1.3.1 完全対応 — `paramRow` の全入力にラベル関連付け追加**: `paramRow()` が生成するすべての入力要素（range・enum select・checkbox・color picker）に `id='pr-{param}'` を付与し、対応する `<label>` の `for` 属性を設定。ラベルをクリックするとフォーカスが対応するスライダー/セレクト/チェックボックス/カラーピッカーに移動するようになった（Round 11 の Exportタブ対応と合わせて全タブで充足）。
- **Content Security Policy 追加**: `<meta http-equiv="Content-Security-Policy" content="object-src 'none'; base-uri 'none'; form-action 'none'">` を追加。インラインスクリプトを持つ単一 HTML の制約内で適用可能な最大 CSP（Flash/ActiveX 禁止・base タグ書き換え禁止・form 外部送信禁止）を設定。
- **ブラウザタブタイトル動的更新**: タイトル入力欄を変更すると `document.title` が `雛 — {ファイル名}` に即時更新されるようになった。複数タブで異なるアバターを作業する際のタブ識別が容易になる。
- **Ctrl+S ヒント表示**: Export タブの書き出しボタン直下に `hint.ctrlS` キーのヒント文（ja/en）を追加。
- **WCAG 1.3.1（情報と関係性）対応 — Exportタブのラベル関連付け**: `txt()` / `selRow()` ヘルパーの `<label>` 要素に `for` 属性、対応する `<input>`/`<select>` に `id` を追加（`meta-title`・`meta-author` 等）。ラベルクリックでフォーカス遷移するようになり、スクリーンリーダーが form 要素を正しく識別できるようになった。
- **Ctrl/Cmd+S キーボードショートカット**: グローバル `keydown` ハンドラを追加し、テキスト入力欄以外でCtrl+S（macOSはCmd+S）を押すとVRM書き出しを即時実行。制作ツールの筋肉記憶ショートカットに対応。
- **WCAG AA コントラスト比修正**: `--text-faint` を `#5f6b78`（コントラスト比 ~2.5:1）から `#7a868f`（~4.7:1）に引き上げ。CSS変数1点の変更で `.row label`・`.sect`・`.rankBadge .lbl`・`.note`・`.limit` 等の全適用箇所がWCAG AA（4.5:1）を充足。
- **スマートデフォルトファイル名**: `title` 未入力時のVRM/JSONファイル名フォールバックを `hina` から `hina_<プリセットID|custom>` に変更（例: `hina_kotone.vrm`）。`fnameStem()` 関数を導入し `doExport()`/`saveJson()` 双方で使用。Exportタブのタイトル欄下にリアルタイムファイル名プレビューを追加（`out.filename` i18nキー、ja/en）。テスト202→205で担保。
- **VRMメタ `contact`/`reference` フィールドをUIに追加**: Exportタブにアバター作者の連絡先（`contactInformation`）と参照元（`reference`）の入力欄を追加。VRM 0.x 仕様の帰属フィールドがUIから設定可能になり、二次配布・権利管理の機械可読化を実現。`out.contact`・`out.reference`・各 placeholder の i18n 4キー（ja/en）追加。テスト200→202で担保。
- **JSON読込失敗のサイレント失敗を解消**: `deserialize()` が null を返した場合（形式不正・`app` フィールド不一致・空ファイル等）、UIが無音のまま変化しなかった。`err.loadFailed` i18n キー（ja/en）を追加し `alert()` で明示通知するよう修正。テスト担保。
- **`saveState()` の過剰 localStorage 書き込みを抑制**: スライダー操作中に毎刻 `localStorage.setItem` が同期実行されていた（height 全可動域で最大 120 回）。500ms デバウンスを追加し最終値のみを永続化。プレビュー `rebuild()` は引き続き即時実行（UX維持）。
- **WCAG 2.1.1（キーボード操作）対応**: 3Dプレビューがポインタ操作のみでキーボード非対応だった問題を修正（SPEC §7のWCAG AA主張の未達ギャップ）。canvasを `tabindex="0"` でフォーカス可能にし、矢印キー=回転／`+``−`=ズーム／`Home`=リセットのキー操作と、言語連動の `aria-label` を追加。ヒント文も更新。
- **WCAG 4.1.3（ステータスメッセージ）対応**: 性能ランクや書き出し結果が視覚的にのみ更新され、支援技術へ伝わらなかった問題を修正。視覚的に隠した `aria-live="polite"` ライブリージョン（`#srStatus`）を追加し、(1) ランク文字列が**実際に変化した時のみ**「性能ランク — PC: … / Quest: …（律速: …）」を通知（スライダー1刻みごとの読み上げ氾濫を重複排除で抑止）、(2) VRM書き出し成功時にファイル名とサイズを通知。
- **localStorage に `activePresetId` / `lastGachaSeed` を永続化**: ページリロード後にプリセット選択ハイライトが失われ、ファイル名が `hina_custom.vrm` にフォールバックする問題を修正。`saveState()` に両フィールドを追加し、`loadState()` では `Number.isFinite()` で `lastGachaSeed=0` を正しく復元。テスト230→232で担保。
- **`beforeunload` で saveState をフラッシュ**: 500ms デバウンス中にタブを閉じると最後の編集が保存されない問題を修正。`beforeunload` ハンドラで保留タイマーをキャンセルし即時 `localStorage.setItem` を実行。テスト232→233で担保。
- **JSON 読込後の残留プリセットハイライト修正**: JSON ファイルを読み込んだ後も前に選択していたプリセットカードのハイライトが残り、ファイル名も旧プリセット ID（例: `hina_kotone.vrm`）のまま書き出される問題を修正。JSON 読込成功時に `activePresetId=null` / `lastGachaSeed=null` をリセットし `saveState()` を呼出。テスト233→234で担保。
- **リセットボタンの完全リセット**: リセットボタンがパラメータのみリセットし、meta（タイトル・作者等）・プリセット選択状態・ガチャシードが残留する問題を修正。`META_DEFAULTS` 定数を導入しリセット時に `meta=Object.assign({},META_DEFAULTS)` / `activePresetId=null` / `lastGachaSeed=null` / `saveState()` を実行。テスト234→235で担保。
- **VRM書き出しエラーメッセージの i18n 対応 + 二重書き出し防止**: `doExport()` のエラー時アラートがハードコード英語（`'Export failed: '`）だった問題を修正。`err.exportFailed` i18n キー（ja/en）を追加し言語連動。また Ctrl+S の高速連打で非同期書き出しが重複起動する問題を `_exporting` フラグで防止。テスト235→237で担保。
- **プリセットカードに `aria-pressed` 追加（WCAG 4.1.2）**: 選択中のプリセットが視覚的（`.selected` CSS クラス）のみで表現され、スクリーンリーダーに伝わらなかった問題を修正。`aria-pressed="true/false"` を各プリセットカードボタンに追加。テスト237→239で担保。WCAG 3.1.1 の `<html lang>` 動的更新（`applyLang()` 内 `document.documentElement.lang = lang`）もテスト追加で担保。
- **統計テーブルのセマンティクス修正（WCAG 1.3.1）**: 統計テーブルのラベル列（三角形数・ボーン数・PC/Questランク等）が全て `<td>` だった問題を修正。`<th scope="row">` に変更し、スクリーンリーダーが行ヘッダーと値の関係を正しく解釈できるように。CSS も `th` を対象に更新。テスト239→240で担保。
- **JSON ファイルのドラッグ＆ドロップ読込**: ブラウザウィンドウ上に `.hina.json` ファイルをドロップするとパラメータを読み込むようになった。`dataTransfer.types.includes('Files')` チェックにより、3D プレビューのドラッグ操作（回転）と衝突しない。`.json` 以外のファイルは `err.loadFailed` で通知。不使用の `.gacha-seed` CSS クラスも削除。テスト240→242で担保。
- **ブラウザタブタイトルを `rebuild()` で即時更新**: JSON 読込・プリセット選択・ガチャ後にタイトルを設定した場合、Exportタブを開くまでブラウザタブタイトルが更新されなかった問題を修正。`rebuild()` に `document.title='雛 — '+fnameStem()` を追加し、アバター再ビルドのたびに即時反映。テスト242→243で担保。
- **Page Visibility API でアニメーションループを一時停止**: ブラウザタブが非表示の間も rAF ループが CPU/GPU を消費していた問題を修正。`visibilitychange` イベントで `_rafPaused` フラグを制御し、ページ非表示中は `renderFrame()` をスキップ。モバイルでの電池消費を低減。テスト243→244で担保。
- **ガチャシード 0 の再現性修正**: シード入力欄が `n>0` ガードでシード 0 を無視していた問題を修正（`mulberry32(0)` は有効な疑似乱数を生成するが再実行不能だった）。`n>=0` に変更。テスト244→246で担保。
- **selfTest 強化 — UV 範囲・法線正規化チェック追加**: `selfTest()` に UV 座標が \[0,1\] 内か・法線ベクトルが単位長か、の2件を追加（20→22件）。UV 範囲逸脱（テクスチャ境界外サンプリング）や法線正規化バグ（陰影計算破綻）を早期発見できる。テスト数は変化なし（selfTest 閾値更新のみ）。
- **WebGL コンテキスト回復時の自動リロード**: `webglcontextlost` の `preventDefault()` でコンテキスト回復を許可していたが、`webglcontextrestored` ハンドラがなかったためリソースが無効のまま停止する問題を修正。回復イベントで `location.reload()` を実行し、localStorage から状態を復元しながら完全再初期化。テスト246→247で担保。
- **`activePresetId` の復元時に有効性を検証**: localStorage から復元した `activePresetId` が廃止されたプリセット ID（バージョン間の変更等）の場合、`hina_obsolete.vrm` のような不正なファイル名が生成されていた。`HINA.PRESETS.some(p=>p.id===j.activePresetId)` で存在確認してから復元するように修正。テスト247→248で担保。
- **カメラ距離を身長変更に比例追従**: プリセット切替・ガチャ等で身長が変わっても `camDist` が初期値のまま固定され、低身長アバターが遠く見えすぎる問題を修正。`uploadGeometry()` 内で `prevH`（旧身長）と `H`（新身長）の比率で `camDist` を比例スケーリング。ユーザーが手動ズームした場合もその比率を維持。テスト248→249で担保。
- **`?` キーで About ダイアログを開くショートカット追加**: テキスト入力欄以外で `?` を押すと About ダイアログが開くようになった。`Ctrl+S` と同じグローバルキーダウンハンドラに統合。テスト担保。
- **カラースウォッチボタンに `type="button"` 追加**: `<button>` 要素は `type` 未指定時に `type="submit"` がデフォルト。現在はフォーム要素がないため実害はないが、将来のHTML変更に対する防御的コーディング。テスト担保。
- **アバターバージョン `meta.version` フィールドをUIに追加**: VRM 0.x メタの `version` フィールド（アバターのバージョン番号。例: 「1.0」「2.3」）が UI から設定できず、常に '1.0' に固定されていた。`out.version` 入力欄と `out.version.ph` プレースホルダー i18n（ja/en）を追加。`META_DEFAULTS` に `version:''` を追加しリセット対象に。VRM ライターの `str(meta.version,'1.0')` が既に対応済みのため追加ロジック不要。テスト257→260で担保。ライセンスオプションテストに `'Other'` を追加して `licenseOpts` 配列を完全化。
- **`otherLicenseUrl` フィールドをUIに追加**: ライセンスを `Other` に設定した場合、VRM ライターは `otherLicenseUrl: ''` をハードコードしていたため、カスタムライセンスの URL を指定する方法がなかった。`out.license.url` 入力欄（`meta.licenseUrl` に連動）を Export タブに追加し、VRM ライターに `str(meta.licenseUrl,'')` で渡すように変更。`META_DEFAULTS` に `licenseUrl:''` を追加。i18n キー `out.license.url`・`out.license.url.ph`（ja/en）追加。テスト254→257で担保。
- **テクスチャラップモードを `REPEAT` → `CLAMP_TO_EDGE` に変更**: `uploadTexture()` が `gl.REPEAT` を使用していたため、バイリニアフィルタリングによりテクスチャアトラスの境界ピクセルが対辺から色を混ぜる「エッジブリード」が発生しえた。UV 座標は [0,1] 内（selfTest 検証済み）なので `CLAMP_TO_EDGE` が正しい設定。テスト担保。
- **selfTest「springボーンノードインデックス範囲」チェックの完全化**: テスト名が「spring bone node indices in range」なのに `colliderGroups` の `node` しか検証せず、`boneGroups[0].bones`（スプリングボーンルートのノードインデックス配列）が未チェックだった。両方を検証するよう拡張。selfTest の件数変化なし（既存テストの強化）。
- **`HINA.M.clamp` → `M.clamp`（コード一貫性）**: `poseAndSkin()` のばね物理部分で `HINA.M.clamp` を使用していたが、ファイル冒頭で `const M = HINA.M` を宣言済みのため `M.clamp` が正しい表記。テスト担保。
- **ライセンス選択に `Other`（その他）を追加**: Export タブのライセンスドロップダウンから `Other` を選択できなかった（VRM ライターは `'Other'` を有効値として受理するにもかかわらず UI から設定不能）。`Other` オプションと `license.Other` i18n キー（ja: 「その他」/ en: 「Other」）を追加し、両者の整合を実現。テスト250→252で担保。
- **カメラドラッグ中の視線更新を停止・視線角度をクランプ**: `pointermove` ハンドラがポインターキャプチャ中（ドラッグ=カメラ回転中）も `gazeX`/`gazeY` を更新し続けていた。(1) カメラ回転中にアバターの目がカーソルを追って不自然に動く (2) キャンバス外に大きくドラッグすると `gazeX/gazeY` が [-1,1] を大きく超え目が極端な方向を向く問題を修正。ドラッグ中はガチャ更新をスキップ（`if (!drag){...return;}`）し、更新時は `M.clamp(..., -1, 1)` でクランプ。テスト249→250で担保。

## [0.1.0] - 2026-06-11
### Added
- パラメトリック人型アバター生成（体格・顔・髪・服・色 30+パラメータ）
- VRM 0.x 書き出し（純粋JS実装・依存ゼロ・GLB+VRM拡張）
  - Humanoid 21ボーン（eye含む・T-pose正規化・Z-向き）
  - BlendShape 17グループ（A/I/U/E/O/Blink/Blink_L/R/Joy/Angry/Sorrow/Fun/Neutral/Look4方向）
  - SpringBone（髪揺れ）+ 頭部コライダ
  - MToon マテリアル（単一マテリアル・単一アトラス・Cutout）
- WebGLリアルタイムプレビュー（CPUスキニング・トゥーン2階調・輪郭線・呼吸/瞬き/視線追従/髪物理）
- VRChat Performance Rank推定（PC/Quest 全14項目・公式2026-04基準値）
- Quest Excellentモード（揺れ物オフ切替）
- プリセット6体 + ガチャ / パラメータJSON保存・読込 / 自動保存
- 日英i18n / かんたん・詳細モード / prefers-reduced-motion対応
- アプリ内 VRChat導入5手順（単体配布時もdocs/不要）
- VRMサムネイル自動埋込（書き出し時のプレビュー視点・256px）
- Nodeテストスイート（依存ゼロ・GLB構造バリデータ内蔵・ノード到達性検証）
### Changed
- firstPersonBoneOffset を実寸計算に変更（目の高さ・顔向きZ-。体格非依存で正確）
### Security
- 完全ローカル動作。外部送信・外部依存・PII収集なし
