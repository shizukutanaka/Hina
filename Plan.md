# Plan.md — 雛 (Hina) v0.1.0

## プロダクト定義（C11・全判断の基準）

このプロダクトは3Dモデリング経験のないVRChatユーザーが、ブラウザ上でパラメータ操作のみによりVRChat対応アバター（VRM 0.x）を作成・出力するための単一HTMLツールである。完全ローカル動作・外部依存ゼロ・外部送信なしを設計上の制約とする。

## 目的

VRoid/Blender/Unityの学習コストなしに、性能ランク対応済みのVRMアバターを数分で取得可能にする。既定プリセットで Quest Good（揺れ物オフで Quest Excellent）/ PC Excellent を保証する。

## スコープ

含む:
- パラメトリック人型生成（体格・顔・髪・服・色 30+パラメータ / プリセット6体 / ガチャ）
- VRM 0.x 書き出し（GLB・Humanoid 21ボーン・BlendShape 17・SpringBone・MToon単一マテリアル）
- WebGLプレビュー（CPUスキニング・トゥーン2階調・輪郭線・呼吸/瞬き/視線追従/髪Verlet物理）
- Performance Rank推定（PC/Quest 全14項目・公式2026-04基準値）
- 日英i18n / かんたん・詳細モード / JSON保存読込 / localStorage自動保存
- Nodeテストスイート（GLB構造バリデータ内蔵）+ CI

含まない（→ docs/SPEC.md §8-9 ロードマップ）:
- VRChatへの直接アップロード（規約上SDK限定・恒久非対応）
- 指ボーン・テクスチャ手描き・FBX出力・VRM 1.0・既存モデル読込

## 規模判定

XL（新プロダクト立ち上げ・推定 index.html 3500行+ / tests 500行）→ Plan.md必須・フルパイプライン。
アーキ/課金/認証変更なし → 取締役会付議は「GitHub公開可否」のみ（zip確認後にユーザー判断）。

## STRIDE（簡略適用）

完全ローカル・単一ユーザー・通信ゼロのため攻撃面は限定的。
- T(改ざん)/I(漏洩): 通信0リクエスト・PII収集なしで構造的に排除（テストでfetch/XHR文字列不在を担保）
- D: 該当なし（サーバーなし）
- 残リスク: localStorage破損 → try/catchで黙ってスキップ（F-012）

## フェーズ + DoD

| # | フェーズ | DoD | 状態 |
|---|---------|-----|------|
| P1 | 調査 | Rank表（公式2026-04）・VRM0座標仕様（Z-向き/右=+X）確定 | 完了 |
| P2 | 完成形ドキュメント | README/SPEC/FAQ/UPLOAD_GUIDE/ADR/LICENSE/CONTRIBUTING/SECURITY/CHANGELOG/CI | 完了 |
| P3 | Plan.md | 本書承認 | 完了 |
| P4 | コア実装 | hina-core（math/geometry/rig/morph/atlas/GLB/VRM/rank/presets/i18n）DOM非依存 | 完了（selfTest 18/18） |
| P5 | UI/プレビュー実装 | WebGL描画・タブUI・書き出し動線・自動保存 | 完了 |
| P6 | テスト | `node tests/run.js` 全通過（SPEC §10 DoD全項目） | 完了（166/166） |
| P7 | 出荷 | hina-v0.1.0.zip 生成・整合性確認 | 完了 |

実測（2026-06-11）: index.html 1979行 / 既定 1929tris・1347verts・29bones / 全6プリセット 1725–1929tris・PC Excellent / Quest: bob・short=Excellent、他=Good（springOffでExcellent）

## 完成形ファイル一覧

```
hina/
├── index.html              # 全実装（build/ 結合生成物）
├── build/                  # ソース分割（00-head / 10-core-a / 11-core-b / 12-core-c / 20-app）
├── Plan.md                 # 本書
├── README.md / LICENSE / CHANGELOG.md / CONTRIBUTING.md / SECURITY.md / CLAUDE.md / .gitignore
├── docs/
│   ├── SPEC.md             # 仕様書（完成形定義・実装と乖離時はこちらが正）
│   ├── FAQ.md
│   ├── UPLOAD_GUIDE.md     # VCC→UniVRM→VRM Converter→SDK 手順
│   └── adr/ADR-001-architecture.md
├── tests/run.js            # Node零依存・GLBバリデータ内蔵
└── .github/workflows/ci.yml
```

## リスク

| リスク | 対策 |
|--------|------|
| VRM0座標系の解釈誤り（180°逆向き） | vrm.dev一次情報で確定済（Z-向き・右=+X）。テストでleftUpperArm.x<0を担保 |
| UniVRM読込失敗（accessor不整合等） | GLB自己バリデータをテストに内蔵。min/max必須項目を機械検証 |
| Quest Excellent割れ | 既定6プリセット全てで tris<7500/bones<75/mat=1 をテストで固定 |
| morph全配列によるファイル肥大 | v0.1は許容（~1.5MB）。v0.2でsparse化（SPEC §9） |
| ブラウザ間WebGL差異 | WebGL1機能のみ使用。uniform上限回避のためCPUスキニング採用（ADR-001） |

## ソクラテス問答記録（2026-06-11・v0.1.0 CrossReview Critic）

| 種別 | 問い | 結論 → 対応 |
|------|------|------------|
| 明確化 | 「VRChat対応」の成立条件は？ | UniVRM読込→Converter変換→SDK上传の全鎖。書式はGLB自己バリデータで機械検証済 |
| 前提検証 | 「テスト通過=UniVRM読込可」は真か？ | Unity実機は本環境で検証不能=残リスク。バリデータにノード到達性等を追加し縮小。初回読込検証はユーザー環境で実施 |
| 根拠 | Rank表示の根拠は正確か？ | pbComp=チェーン根数・pbTrans=チェーンボーン数=Converter変換後実数と一致。texMBは非圧縮値で保守的 |
| 視点転換 | index.html単体配布ユーザーはdocs/を持たない | 導線断絶 → アプリ内（出力タブ）に導入5手順を内蔵（i18n） |
| 帰結 | 1年後の保守者は何に苦しむ？ | worldMats汚染・死式 → 浄化。コンテキストロスト時の無言死 → 通知追加 |
| メタ | 最も価値ある未解決問題は？ | meta.texture=アトラス流用はツール上で破綻表示 → プレビュー視点の256pxサムネイル自動埋込。firstPersonBoneOffsetを実寸（目高・顔向きZ-）で精密化 |

結論3往復で収束。Unity実機検証のみ人間側タスクとして残置。

## ソクラテス問答記録（2026-06-13・アクセシビリティ深掘り）

| 種別 | 問い | 結論 → 対応 |
|------|------|------------|
| 明確化 | SPEC §7「WCAG AA」の成立条件は？ | キーボード操作(2.1.1)に加え、状態変化の支援技術伝達(4.1.3 Status Messages)もAA要件 |
| 前提検証 | 「スライダーを動かせば結果が分かる」は全ユーザーに真か？ | 偽。ランク/統計はcanvas横に視覚更新のみ。`#rankWrap`に`aria-live`なし → SR利用者はGood→Excellentの変化を知覚不能 |
| 視点転換 | 全盲ユーザーは「揺れ物オフでQuest Excellent」をどう確認？ | 確認手段なし。物理トグル操作が無言。律速明示の設計意図が視覚限定で破綻 |
| 根拠 | 書き出し成功のフィードバックは？ | 無言（ファイルが落ちるだけ）。失敗時のみalert。SR利用者に成功が伝わらない |
| 帰結 | 全状態変化を読み上げると？ | スライダー1刻みごと＝騒音。ランク文字列が実変化した時のみ通知する重複排除が必須 |
| メタ | 最小コスト最大是正は？ | 視覚的に隠した単一`aria-live`領域＋(1)ランク変化の重複排除通知 (2)書き出し成功通知。i18n＋テストで担保 |

結論2往復で収束。WCAG 2.1.1(前回)＋4.1.3(今回)でSPEC §7のAA主張のUI操作系ギャップを解消。

## ソクラテス問答記録（2026-06-13・データ整合性・サイレント失敗）

| 種別 | 問い | 結論 → 対応 |
|------|------|------------|
| 明確化 | 英語ユーザーがExportタブで見る`note.upload`の内容は？ | "See docs/UPLOAD_GUIDE.md" と表示される。単体配布にそのファイルは存在しない。CHANGELOGに記載済みの日本語修正と対称的に英語オブジェクトにも同じ死エントリが残存 → 両言語の旧エントリを削除。正エントリ（最後尾・上書き）はすでに存在していた |
| 前提検証 | JSON読み込み失敗時にユーザーは何を知覚できるか？ | 知覚できない。`deserialize()`がnullを返すと`if(d)`はfalseで無音のままUIは変化なし。操作が届いたか分からない → `err.loadFailed` i18nキー追加・alertによる明示通知 |
| 根拠 | `saveState()`はスライダー1刻みで何回`localStorage.setItem`を呼ぶか？ | height 0.8→2.0を引くと120回の同期setItem。mid-drag中間値はほぼ無価値 → debounce(500ms)で最終値のみ永続化 |
| 視点転換 | debounce化で即時性が失われる場所はあるか？ | 言語・モード切替は明示的意図だが500ms以内の離脱は稀。blanket 500ms debounceで許容範囲内 |
| 帰結 | `rebuild()`もdebounceすべきか？ | 否。プレビュー即時更新がこのツールの主要UX。ターゲット（デスクトップ）では十分高速。saveStateのみdebounce |
| メタ | 3つを合わせた最小コスト最大是正は？ | (1)両言語の死エントリ削除 (2)`err.loadFailed`+alertフィードバック (3)saveState 500ms debounce。合計20行未満・テスト178→183で担保 |

結論3往復で収束。i18n重複エントリ・サイレント失敗・過剰LocalStorage書き込みの3問題を同時解消。

## ソクラテス問答記録（2026-06-13・エクスポート品質・回復力）

| 種別 | 問い | 結論 → 対応 |
|------|------|------------|
| 明確化 | Exportタブのライセンスドロップダウンに実際に表示される文字列は？ | `Redistribution_Prohibited`、`CC_BY_NC_SA` 等のraw技術値。VRC初心者には難解。`selRow` の第4引数が `null` のため翻訳が無効化されていた → `license.*` i18n 8キー追加＋引数を `'license'` に変更 |
| 前提検証 | `buildAvatar()` が例外を投げた場合、何が起きるか？ | `build` が null のまま、UIは無音で機能停止。エラーバウンダリなし → `rebuild()` に try/catch を追加。`err.buildFailed` i18n キーで alert 通知 |
| 視点転換 | タイトル未入力でエクスポートすると？ | `hina.vrm` 固定。複数バリアント試行時に全ファイルが同名 → title/author 入力欄に placeholder ヒント追加（`out.title.ph`・`out.author.ph`） |
| 根拠 | VRMメタ `version` フィールドの仕様上の意味は？ | アバター製作者バージョン文字列。空文字列はUniVRM通過可能。v0.2課題として残置 |
| 帰結 | 3問題の優先順位は？ | (1)ライセンス表示ローカライズ — 全エクスポーターに影響 (2)error boundary — 稀だが致命的 (3)placeholder — UXヒント |
| メタ | 最小コスト最大是正は？ | (1)8×2 `license.*` i18nキー+selRow引数変更 (2)`err.buildFailed`付きtry/catch (3)placeholder 2キー追加。合計30行・テスト183→190で担保 |

結論3往復で収束。エクスポートUI品質・回復力・入力ヒントの3軸を同時改善。

## ソクラテス問答記録（2026-06-13・タブナビゲーション・ARIA完全準拠）

| 種別 | 問い | 結論 → 対応 |
|------|------|------------|
| 明確化 | `role="tablist"` を名乗るために何が必要か？ | WAI-ARIA Authoring Practices 1.2: tablist内の各buttonに `role="tab"` `aria-selected` `tabindex`（active=0/非active=-1）、tabpanelに `aria-labelledby`。いずれも未実装 |
| 前提検証 | 現在のタブ切替はキーボードから操作可能か？ | 不可。全tabに `tabindex` 不在のため、Tabキーで全ボタンが順番に獲得フォーカスし、ARIAパターン違反。矢印キー操作も無い |
| 根拠 | クリック操作でフォーカスが次のタブに残るか？ | 否。`onclick` は `activeTab` を更新してリフォームするが `focus()` を呼んでいないため、DOMが差し替わって以前のDOM要素を参照するフォーカスが失われる |
| 視点転換 | `tabindex` をレンダリング外の1箇所で管理するリスクは？ | renderTabs() が呼ばれるたびに全tabを作り直す構造のため、roving tabindex（active=0/非active=-1）をrenderTabs内で設定するのが自然で一貫 |
| 帰結 | ArrowKey handler はどこに置くべきか？ | boot時1回の `$('tabs').addEventListener('keydown',…)` で済む。DOMを差し替えてもイベントは親要素(`#tabs`)に委譲されるため安全 |
| メタ | 最小コスト最大是正は？ | renderTabs(): role=tab + aria-selected + roving-tabindex + 切替後focus()。boot: keydown委譲(ArrowL/R/U/D + Home/End)。tabBody: role=tabpanel + aria-labelledby。テスト190→197で担保 |

結論3往復で収束。ARIA tablistパターンを完全準拠に更新し、キーボードのみでタブ全操作を可能にした。

## ソクラテス問答記録（2026-06-13・プリセット選択状態・再現性）

| 種別 | 問い | 結論 → 対応 |
|------|------|------------|
| 明確化 | ユーザーがプリセットを選んだ後にスライダーを動かすと、どのプリセットが「選択中」と見えるか？ | 全プリセットカードが未選択表示のまま。`activePresetId` 変数が存在せず、「Kotone を選んだ後に身長を変えても Kotone が選択状態として光る」という期待を満たせない |
| 前提検証 | ガチャ結果は再現可能か？ | 不可。シードが一時変数に保存されず、ガチャ後に別タブを開いてもシード値が消える。再実行は不可能 |
| 根拠 | `onParam()` をデバウンスしないとプリセット選択状態のリセットタイミングは？ | 即時リセット（正しい）。ただし変数が存在しないためreseti後もUI上では全カードが無選択のままで差がない |
| 視点転換 | 「どのプリセットから派生したか」の情報はVRM書き出しに影響するか？ | 書き出しには影響しない。あくまでUI操作コンテキストの視覚フィードバック。ただしsaveStateに含める必要もない（ページリロード時にはpreset状態を復元不要） |
| 帰結 | ガチャのシードUIとして何が最小実装か？ | `lastGachaSeed` 変数に保持→ガチャ実行後に seed 番号を小テキストで表示。入力欄は過剰。将来的にURLパラメータで共有可能な形式に |
| メタ | 最小コスト最大是正は？ | (1)`activePresetId` 変数でプリセット選択ハイライト (2)`onParam()` でリセット (3)`lastGachaSeed` 表示 (4)CSS `.preCard.selected` + `.gacha-seed`。テスト197→200で担保 |

結論3往復で収束。プリセット選択フィードバックとガチャ再現性の2軸を最小コストで改善。

## ソクラテス問答記録（2026-06-13・VRMメタ完全性）

| 種別 | 問い | 結論 → 対応 |
|------|------|------------|
| 明確化 | VRM 0.x `extensions.VRM.meta` の必須フィールドと任意フィールドは何か？ | 必須: `title`（UniVRM許容だが空は非推奨）・`author`。推奨任意: `contactInformation`（=contact）・`reference`。全て文字列。UIに出ていないフィールドはVRM側で空文字のまま |
| 前提検証 | `contact` と `reference` が常に空のままVRMを配布する問題は？ | 二次配布者が元制作者に連絡する手段がなくなる。VRChat Worldに配置時、アバター同定が困難。VRM意図（帰属の機械可読化）が機能しない |
| 根拠 | UniVRM は `contactInformation` 空文字をエラーにするか？ | しない。任意フィールドなので互換性問題はない。ただし「存在は知っているが入れる場所がない」というUX問題 |
| 視点転換 | ExportタブはmetaフィールドをどこまでUIに出すべきか？ | title/authorは既存。contact/referenceはユーザーの制作背景に依存するが入力機会は提供すべき。violent/sexual/commercial許可フラグはv0.1スコープ（すでに実装済み） |
| 帰結 | `version`（アバターバージョン文字列）もUIに出すか？ | v0.1スコープ外。空文字のままUniVRM通過可能。管理コストに見合う頻度の低い入力 |
| メタ | 最小コスト最大是正は？ | `meta` オブジェクトに `contact`/`reference` フィールド追加。Export UIに2行のtxt()追加。i18n 4キー（ja/en）追加。VRMライター側はすでに `meta.contact`/`meta.reference` を使うか確認→利用済み。テスト202で担保 |

結論2往復で収束。VRMメタ帰属フィールドをUIに露出し、権利関係の機械可読化を実現した。

## ソクラテス問答記録（2026-06-13・カラーアクセシビリティ・WCAG コントラスト比）

| 種別 | 問い | 結論 → 対応 |
|------|------|------------|
| 明確化 | WCAG AA のテキストコントラスト基準は？ | 通常テキスト(14px/非bold等) = 4.5:1 / 大テキスト(≥18pt or 14pt bold) = 3:1 / UIグラフィック境界 = 3:1 |
| 前提検証 | `--text-faint: #5f6b78` on `--bg: #0F1216` のコントラスト比は？ | 約2.5:1 — WCAG AA(4.5:1)に未達。`.row label`(12px)・`.sect`・`.rankBadge .lbl`・`.note`・`.limit`等で広く使用 |
| 根拠 | `--text-dim: #9aa6b2` は合格するか？ | 約6.3:1 — 合格。`--text-faint` のみが違反 |
| 視点転換 | 「淡い」見た目を維持しつつ AA を達成できる最小の値は？ | bg (#0F1216, 輝度≈0.007) に対し 4.5:1 = 必要輝度≥0.038 → #7a868f (輝度≈0.210/bg比≈4.7:1) で視覚的淡さを保ちながらギリギリAA達成 |
| 帰結 | CSS変数1点変更で全箇所が修正されるか？ | Yes。`--text-faint` を `#5f6b78` → `#7a868f` に変更するだけ。`.sw`・`.c` の `rgba(255,255,255,.15)` 装飾ボーダーはグラフィック装飾のため適用外 |
| メタ | 最小コスト最大是正は？ | CSS 1行変更 + テスト1行追加で担保。テスト205→206 |

結論2往復で収束。`--text-faint` 値1点の修正でWCAG AAコントラスト要件を全適用箇所で充足した。

## ソクラテス問答記録（2026-06-13・キーボードショートカット・パワーユーザーUX）

| 種別 | 問い | 結論 → 対応 |
|------|------|------------|
| 明確化 | 制作ツールで最も重要なキーボードショートカットは？ | Ctrl/Cmd+S（保存/書き出し）。ブラウザの「ページ保存」動作をVRMコンテキストで上書きする |
| 前提検証 | Ctrl+S を preventDefault してもページ機能は損なわれないか？ | 損なわれない。「ページを保存」はVRMツールでは無意味。preventDefault + doExport() への置換は安全 |
| 根拠 | Ctrl+S はVRM書き出しかJSON保存か？ | VRM書き出しが主作成物。JSON保存は副次的。Ctrl+Sはより価値の高い操作に割り当て |
| 視点転換 | テキスト入力欄でCtrl+Sが押された場合は？ | ブラウザ既定のコピペ動作と干渉しないよう、activeElement が INPUT/SELECT/TEXTAREA の時はフォールスルー |
| 帰結 | グローバル `document.addEventListener('keydown')` は適切か？ | Yes。フォーカスに関係なくどこからでも発動するのが期待動作 |
| メタ | 最小コスト最大是正は？ | boot 部に7行追加。テスト206→207 |

結論2往復で収束。Ctrl/Cmd+S で即時VRM書き出し。パワーユーザーのワークフローが大幅に改善。

## ソクラテス問答記録（2026-06-13・WCAG 1.3.1 form 要素ラベル関連付け）

| 種別 | 問い | 結論 → 対応 |
|------|------|------------|
| 明確化 | WCAG 1.3.1「情報と関係性」における form 要素の要件は？ | `<input>`/`<select>` は対応する `<label>` に `for` 属性でプログラム的に関連付けるか、`aria-label`/`aria-labelledby` が必要 |
| 前提検証 | Exportタブの `<label>` 要素は入力欄と関連付けられているか？ | 否。`txt()` / `selRow()` 両ヘルパーで label は `for` なし、input/select は `id` なし。ラベルクリックでフォーカス遷移しない |
| 根拠 | `paramRow()` の label はどうか？ | 全入力に `aria-label` が付与されているため SR は読める。ただしラベルクリックでフォーカスは移らない |
| 視点転換 | Exportタブのみ優先的に修正する理由は？ | `paramRow` は `aria-label` で SR 対応済み。Export の `txt()` / `selRow()` は `aria-label` も `for` もない → 即時修正が必要 |
| 帰結 | 最小の修正は？ | `txt()`: input に `id='meta-'+mk`、label に `for=id`。`selRow()`: select に `id='sel-'+mk`、label に `for=id`。title 行にも `id='meta-title'` を追加 |
| メタ | コスト/効果 | 5行変更・ゼロリスク・WCAG 1.3.1 達成・テスト207→209 |

結論2往復で収束。Exportタブの全 form 要素でラベルのプログラム的関連付けを確立した。

## ソクラテス問答記録（2026-06-13・ブラウザタブ識別・UX細部）

| 種別 | 問い | 結論 → 対応 |
|------|------|------------|
| 明確化 | 複数タブで異なるアバターを作業する場合、タブはどう識別されるか？ | 全タブ「雛 — VRM Avatar Maker」固定。区別不能 |
| 前提検証 | `document.title` 動的更新の副作用は？ | ゼロ。ブラウザ標準機能。title タグは JS で随時更新可能 |
| 根拠 | Ctrl+S 実装済みなのに UI でその存在をユーザーは知れるか？ | 知れない。Export ボタンにヒントなし |
| 視点転換 | `document.title` 更新の最適タイミングは？ | `updateFnPrev()` 内（title 入力の oninput 時と初期描画時）。fnameStem() を再利用するのが自然 |
| 帰結 | Ctrl+S ヒントはどこに表示すべきか？ | Export ボタン直下。書き出し操作に隣接したコンテキストで伝わりやすい |
| メタ | コスト/効果 | 5行追加・i18n 2キー（ja/en）・テスト209→211 |

結論2往復で収束。タブタイトルの個別識別とCtrl+Sショートカットのディスカバラビリティを同時改善。

## ソクラテス問答記録（2026-06-13・セキュリティ — Content Security Policy）

| 種別 | 問い | 結論 → 対応 |
|------|------|------------|
| 明確化 | CSP を サーバーなし単一 HTML で設定する手段は？ | `<meta http-equiv="Content-Security-Policy">` タグ。サーバーレスポンスヘッダ不要 |
| 前提検証 | Hina に XSS 攻撃面はあるか？ | `innerHTML=''` は空クリアのみ。外部データの innerHTML 展開なし。ただし CSP 未設定 |
| 根拠 | CSP なしのリスクは？ | ブラウザ拡張・ユーザースクリプトの eval()/スクリプト注入。ローカルファイルで実害は限定的だが改ざん配布のリスクは残る |
| 視点転換 | インラインスクリプトを使う単一 HTML で `script-src` は適用できるか？ | `unsafe-inline` なしには自身のスクリプトもブロックされる → `script-src` は適用不可 |
| 帰結 | 適用可能な最大 CSP は？ | `object-src 'none'`（Flash/プラグイン禁止）+ `base-uri 'none'`（base タグ書き換え禁止）+ `form-action 'none'`（form 外部送信禁止） |
| メタ | コスト/効果 | head に1行追加・テスト211→212 |

結論2往復で収束。インライン単一 HTML の制約内で適用可能な最大 CSP を設定し、構造的な攻撃面を縮小した。

## ソクラテス問答記録（2026-06-13・`paramRow` WCAG 1.3.1 完全対応）

| 種別 | 問い | 結論 → 対応 |
|------|------|------------|
| 明確化 | Round 11 で Export タブの label/for を修正したが、パラメータタブは？ | `paramRow()` の全入力（range・select・checkbox・color）で `for`/`id` が未設定。ラベルクリックでフォーカス遷移しない |
| 前提検証 | `aria-label` があれば `for`/`id` は不要か？ | SR は `aria-label` で読み上げ可能だが、マウスでラベルクリックした時のフォーカス遷移（WCAG 1.3.1・マウスUX）は `for`/`id` なしでは不可 |
| 根拠 | paramRow が生成するパラメータ名は一意か？ | PARAMS のキー名（`height`・`eyeSize`・`hairStyle` 等）はグローバル一意 → `pid='pr-'+k` で一意 id を生成できる |
| 視点転換 | color 行では label は color picker と swatches どちらを指すべきか？ | color picker（`<input type=color>`）を primary control として指定。swatches は補助的ボタン群で個別に `aria-label` を持つ |
| 帰結 | bool（checkbox）行: label の `for` が checkbox を指すとラベルクリックで toggle されるか？ | Yes。これが HTML の `<label>` 本来の動作。スペースキーでも toggle 可能になる |
| メタ | コスト/効果 | `paramRow()` 4ケースに各1行の `id:pid` / `'for':pid` 追加。テスト212→213 |

結論2往復で収束。全タブの全パラメータ行でWCAG 1.3.1 を充足した。

## ソクラテス問答記録（2026-06-13・ARIAランドマーク構造）

| 種別 | 問い | 結論 → 対応 |
|------|------|------------|
| 明確化 | スクリーンリーダーのランドマーク移動（NVDA: R）を使うために何が必要か？ | `<header>`/`<main>`/`<nav>`/`<aside>` は暗黙的ランドマーク。`<section>` は `aria-label`/`aria-labelledby` があって初めてランドマーク(region)になる |
| 前提検証 | `<section id="stage">` はランドマークか？ | 否。名前なし `<section>` は SR でランドマークとして認識されない。canvas を含む重要エリアだが SR ユーザーはランドマーク移動でスキップできない |
| 根拠 | `<aside id="panel">` はランドマークか？ | Yes（complementary）。ただし名前なしのため「補足」としか読まれない。名前を付けると「アバター設定パネル」として識別可能 |
| 視点転換 | `aria-label` を HTML に直書きすると言語切替で追従するか？ | 追従しない。`applyLang()` 内で動的に設定することで言語同期を担保 |
| 帰結 | `a11y.stage` と `a11y.panel` の i18n キーを追加し `applyLang()` で設定 | Yes。ゼロコスト・ゼロリスク |
| メタ | コスト/効果 | i18n 4キー + applyLang 2行 + テスト3件。テスト213→216 |

結論2往復で収束。ARIA ランドマーク移動で全主要エリアに到達可能になった。

## ソクラテス問答記録（2026-06-13・ガチャシード再現性の完成）

| 種別 | 問い | 結論 → 対応 |
|------|------|------------|
| 明確化 | ガチャ結果を友人に共有したい場合、現状どうするか？ | シード番号を口頭伝達 → 受け手はシード入力欄がないので再現不能 |
| 前提検証 | `randomParams(seed)` は同じシードで同じ結果を返すか？ | Yes。mulberry32 疑似乱数は決定論的。シード = アバターのレシピ |
| 根拠 | シード入力欄の最小実装は？ | ガチャセクション下部に `<input type=number>` を追加。`onchange` でシードを適用 |
| 視点転換 | `oninput` でリアルタイム再実行すべきか？ | 否。途中の数字（例: "1234" を打ちながら "1"→"12"→"123"→"1234"）のたびに3D再構築が走るのはUX劣化。`onchange`（Enter/blur）が適切 |
| 帰結 | ガチャボタンと種入力を `runGacha(seed)` に統一 | ロジック重複排除。ボタンはランダムseed、入力欄はユーザー指定seed |
| メタ | コスト/効果 | 約15行・i18n 2キー追加・テスト216→218 |

結論2往復で収束。シード番号の共有によるアバター再現性が完成した。

## ソクラテス問答記録（2026-06-13・VRM meta 入力上限と無音切り詰め防止）

| 種別 | 問い | 結論 → 対応 |
|------|------|------------|
| 明確化 | VRM 0.x meta 文字列に上限はあるか？ | VRM ライターの `str()` が `.slice(0,256)` でサニタイズ。256文字超は無音切り詰め |
| 前提検証 | ユーザーは切り詰めに気づけるか？ | 気づけない。エクスポート後の VRM を見るまで分からない |
| 根拠 | `maxlength` 属性でブラウザ入力を上限制限できるか？ | Yes。ブラウザは UI 上で 256 文字以降のキー入力を受け付けなくなる |
| 視点転換 | 全メタフィールドに設定が必要か？ | title・author・contact・reference の全て `str()` を通るため全フィールドに設定 |
| 帰結 | `txt()` と title インライン行に `maxlength:'256'` を追加 | 1箇所変更で全メタフィールドに適用 |
| メタ | コスト/効果 | 2行変更・テスト218→219 |

結論2往復で収束。VRM 書き出しでのサイレント切り詰めを UI 側で未然に防止した。

## ソクラテス問答記録（2026-06-13・WebGL エラーメッセージの i18n 対応）

| 種別 | 問い | 結論 → 対応 |
|------|------|------------|
| 明確化 | WebGL 使用不可時のヒントはローカライズされているか？ | 否。ハードコード英語文字列。日本語モードでも英語表示 |
| 前提検証 | `applyLang()` が `t('hint.drag')` を無条件設定すると何が起きるか？ | WebGL 失敗メッセージが言語切替で上書きされる |
| 根拠 | `hint.drag` はドラッグ操作ヒントであり WebGL 失敗時に表示すべきではない | 正しい。GLOK フラグで条件分岐が必要 |
| 視点転換 | webglcontextlost handler は `t()` を使えるか？ | Yes。クロージャで参照可能 |
| 帰結 | `hint.noGL` / `hint.glLost` i18n キー追加、applyLang() と handler を修正 | 4キー（ja/en）+ 2行修正 |
| メタ | コスト/効果 | テスト219→222 |

結論2往復で収束。WebGL エラー状態の日英ローカライズと言語切替時の整合性を確立した。

## ソクラテス問答記録（2026-06-13・About ダイアログ ARIA 対応）

| 種別 | 問い | 結論 → 対応 |
|------|------|------------|
| 明確化 | `<dialog>` の accessible name はどう決まるか？ | `aria-labelledby` か `aria-label` が必要。なければ SR は「ダイアログ」とだけ読む |
| 前提検証 | native `<dialog> + showModal()` はタイトルを自動取得するか？ | 実装依存・WAI-ARIA は明示的指定を要求 |
| 根拠 | `<h2>` に `id` を付けて `aria-labelledby` で指定 | 「雛 (Hina) v0.1.0 ダイアログ」と読まれるようになる |
| 視点転換 | 閉じるボタンの `OK` テキストは日英切替で追従するか？ | 追従しない。`applyLang()` でセットするか i18n キーを使う必要がある |
| 帰結 | `about.close` i18n キー（ja: 閉じる / en: Close）を追加し `applyLang()` で設定 | ボタンも言語連動 |
| メタ | コスト/効果 | HTML 2行・i18n 2キー・applyLang 1行・テスト222→224 |

結論2往復で収束。About ダイアログが WAI-ARIA dialog パターンを充足した。

## ソクラテス問答記録（2026-06-13・selfTest 強化・bone/node インデックス整合性）

| 種別 | 問い | 結論 → 対応 |
|------|------|------------|
| 明確化 | selfTest は VRM glTF JSON の node 参照が有効かチェックするか？ | 現状: アクセサバイト範囲のみ検証。node インデックス有効性は未チェック |
| 前提検証 | 無効 node インデックスを持つ VRM は UniVRM で読み込めるか？ | 読み込み失敗。VRChat SDK でも動作しない |
| 根拠 | humanoid bone と SpringBone collider が正しいインデックスを指すかは自動検証すべき | Yes。コード変更で壊れうる不変条件のため |
| 視点転換 | これらは現在のコードで壊れうるか？ | 低リスクだが `nodeOf()` のバグや将来の変更で壊れる可能性がある |
| 帰結 | selfTest に2件追加し 18→20 checks | 予防的テスト。テスト閾値も更新 |
| メタ | コスト/効果 | 10行追加・テスト threshold 更新 |

結論2往復で収束。VRM node インデックス整合性が自動検証された。

## ソクラテス問答記録（2026-06-13・`<noscript>` フォールバック）

| 種別 | 問い | 結論 → 対応 |
|------|------|------------|
| 明確化 | JS 無効時にユーザーは何を見るか？ | 空白ページ。説明なし |
| 前提検証 | VRChat ツールに JS 無効ユーザーは来るか？ | セキュリティ重視ユーザーと検索クローラー |
| 根拠 | `<noscript>` はどのブラウザでサポートされているか？ | HTML Living Standard。全ブラウザ対応 |
| 視点転換 | CSS 変数を `<noscript>` 内の style で使えるか？ | Yes。CSS 変数は `<noscript>` が表示される前に `<head>` の `:root` で定義済み |
| 帰結 | `<body>` 開始直後に日英両言語のメッセージを追加 | 1行追加・ゼロリスク |
| メタ | コスト/効果 | HTML 1行追加・テスト224→225 |

結論1往復で収束。JS 無効時のユーザー体験を最小コストで改善した。

## ソクラテス問答記録（2026-06-13・WCAG 2.1.1 キーボード操作対応）

| 種別 | 問い | 結論 → 対応 |
|------|------|------------|
| 明確化 | WCAG AA を主張するが 3D プレビューはキーボードで操作できるか？ | できない。canvas はポインタ操作のみ。`tabindex` がなくフォーカスも当たらない |
| 前提検証 | WCAG 2.1.1 キーボードの適用範囲は？ | 「マウスでできることはキーボードでもできなければならない」。3D プレビューの回転・ズームが対象 |
| 根拠 | canvas にフォーカスを与える最小手段は？ | `tabindex="0"` を追加するだけ。`role="img"` も設定してセマンティクスを明確化 |
| 視点転換 | キーバインドはどこに置くべきか？ | canvas の `keydown` イベント。矢印キー=回転・`+`/`-`=ズーム・`Home`=リセット。デフォルト動作を preventDefault で抑止 |
| 帰結 | `aria-label` は言語切替に追従するか？ | `applyLang()` で `t('a11y.canvas')` を動的設定することで追従。i18n キー `a11y.canvas`（ja/en）を追加 |
| メタ | コスト/効果 | canvas 属性2つ + keydown ハンドラ + i18n 2キー + applyLang 1行。テスト225→227 |

結論2往復で収束。3D プレビューへのキーボードアクセスを確立し WCAG 2.1.1 を充足した。

## ソクラテス問答記録（2026-06-13・WCAG 4.1.3 ステータスメッセージ対応）

| 種別 | 問い | 結論 → 対応 |
|------|------|------------|
| 明確化 | 性能ランクや書き出し完了などの状態変化は支援技術に伝わるか？ | 伝わらない。DOM に数値が書き込まれるが `role` も `aria-live` もないため SR は読み上げない |
| 前提検証 | WCAG 4.1.3 ステータスメッセージとは？ | フォーカスを受けずに表示されるメッセージ（ランク・成功・エラー）を SR が読み上げられること |
| 根拠 | `aria-live="polite"` を持つ視覚的に非表示の領域を追加すれば十分か？ | Yes。`.sr-only` で非表示にした `role="status"` 要素に textContent を設定すれば SR が読む |
| 視点転換 | スライダー操作ごとに毎回読み上げが走ると煩雑ではないか？ | Yes。前回と同じランク文字列の場合は更新しない重複排除が必要 |
| 帰結 | (1)ランク変化時のみ通知 (2)VRM 書き出し成功時にファイル名+サイズを通知 | `#srStatus` に `aria-live="polite"` + `aria-atomic="true"` + 変化検出ロジック |
| メタ | コスト/効果 | HTML 1要素 + JS 変化検出 + 書き出しハンドラ通知追加。テスト227→228 |

結論2往復で収束。スクリーンリーダーユーザーへのランク変化・書き出し結果の通知を確立した。

## ソクラテス問答記録（2026-06-13・localStorage への activePresetId / lastGachaSeed 永続化）

| 種別 | 問い | 結論 → 対応 |
|------|------|------------|
| 明確化 | ページをリロードすると何が復元されないか？ | パラメータ値・meta・言語・モードは復元される。が「どのプリセットが選択中か」「最後のガチャシード」が失われる |
| 前提検証 | プリセット選択状態が消えると UX にどう影響するか？ | プリセットタブでカード未選択（ハイライトなし）・ファイル名が `hina_custom.vrm`（プリセット ID を使えず） |
| 根拠 | `lastGachaSeed=0` の場合に `if (j.lastGachaSeed)` で復元できるか？ | できない。falsy（0）はスキップされる。`Number.isFinite()` による明示的判定が必要 |
| 視点転換 | `saveState()` 内の JSON に追加するコストは？ | ゼロ。既存 `JSON.stringify` に2プロパティを追加するだけ |
| 帰結 | `saveState()` に `activePresetId, lastGachaSeed` を追加。`loadState()` で `Number.isFinite` チェックで復元 | 状態永続性のギャップを最小コストで解消 |
| メタ | コスト/効果 | saveState 1行・loadState 2行・テスト230→232 |

結論2往復で収束。プリセット選択とガチャシードがページリロード後も維持されるようになった。

## ソクラテス問答記録（2026-06-13・beforeunload でのデバウンス flush）

| 種別 | 問い | 結論 → 対応 |
|------|------|------------|
| 明確化 | 500ms デバウンス中にタブを閉じると何が起きるか？ | デバウンスタイマーが発火しないため、最後の操作が localStorage に保存されない |
| 前提検証 | `beforeunload` 内の `localStorage.setItem` はブラウザが保証するか？ | Yes。同期操作はブラウザが完了を保証する（非同期 fetch 等とは異なる） |
| 根拠 | 保留タイマーを cancel して即時保存する実装は？ | `clearTimeout(_saveTimer)` + 同じ `localStorage.setItem` を 1 行で実行 |
| 視点転換 | beforeunload で saveState() 関数を呼べばよいのでは？ | saveState() はデバウンスを再スタートするため不可。ロジックをインライン複製する必要がある |
| 帰結 | `window.addEventListener('beforeunload', ...)` で flush のみ実装 | 関数呼び出しではなくインライン即時実行でデバウンスをバイパス |
| メタ | コスト/効果 | 3行追加・テスト232→233 |

結論2往復で収束。タブを閉じた際のデータ消失をゼロにした。

## ソクラテス問答記録（2026-06-13・JSON読込後の残留プリセットハイライト修正）

| 種別 | 問い | 結論 → 対応 |
|------|------|------------|
| 明確化 | 「kotone」プリセット選択中に他ユーザーから貰った JSON を読み込むと何が起きるか？ | params と meta は上書きされるが `activePresetId` が `'kotone'` のまま。プリセットカードのハイライトが残留 |
| 前提検証 | `fnameStem()` はこの状態でどうなるか？ | `safeName(meta.title, 'hina_'+activePresetId)` → タイトル未入力なら `hina_kotone.vrm` が書き出される（誤り） |
| 根拠 | `saveState()` は JSON 読込後に呼ばれているか？ | 呼ばれていない。読み込んだパラメータがページリロード後に消える |
| 視点転換 | `lastGachaSeed` もリセットすべきか？ | Yes。読み込んだ JSON はガチャ由来でない可能性が高い。seed を保持するのは misleading |
| 帰結 | 読込成功時に `activePresetId=null; lastGachaSeed=null;` を追加し `saveState()` を末尾に呼出 | 1行修正・テスト233→234 |

結論2往復で収束。JSON 読込後のプリセット状態一貫性を確保した。

## ソクラテス問答記録（2026-06-13・リセットボタンの完全リセット）

| 種別 | 問い | 結論 → 対応 |
|------|------|------------|
| 明確化 | リセットボタンは何をリセットするか？ | `params = HINA.defaults()` のみ。meta・activePresetId・lastGachaSeed はそのまま残る |
| 前提検証 | リセット後にタイトル欄やプリセットハイライトはどうなるか？ | 以前入力したタイトル・作者が残る。選択していたプリセットカードがハイライトされたまま |
| 根拠 | meta の初期値を1か所で管理できるか？ | `META_DEFAULTS` const を宣言し、初期化と reset 時の両方で `Object.assign({}, META_DEFAULTS)` を使う |
| 視点転換 | `saveState()` を忘れるとどうなるか？ | リセット後に再読み込みすると元の状態に戻る（リセットが永続しない） |
| 帰結 | `META_DEFAULTS` を導入し、reset handler に meta・activePresetId・lastGachaSeed・saveState() を追加 | 完全なリセットを1ハンドラで実現 |
| メタ | コスト/効果 | 2行変更・テスト234→235 |

結論2往復で収束。リセットボタンがすべてのユーザー状態を初期値に戻すようになった。

## ソクラテス問答記録（2026-06-13・書き出しエラーi18n + 二重書き出し防止）

| 種別 | 問い | 結論 → 対応 |
|------|------|------------|
| 明確化 | `doExport()` でエラーが発生した場合のアラートメッセージは日英に対応しているか？ | 否。`'Export failed: '+e.message` とハードコード英語。日本語モードでも英語表示 |
| 前提検証 | Ctrl+S を素早く2回押した場合、書き出しは何回実行されるか？ | `doExport()` は `async` で await を含む。2回目の呼び出しが1回目の完了前に走る可能性がある |
| 根拠 | 二重書き出しの実害は？ | 同じファイルが2回ダウンロードされ、ブラウザのダウンロード完了通知が2件表示される。UX ノイズ |
| 視点転換 | フラグ管理と `try/finally` の組み合わせは正しいか？ | Yes。`finally` によりエラー時もフラグが必ずリセットされる |
| 帰結 | `err.exportFailed` i18n 追加 + `_exporting` フラグでガード | 2つの独立した小修正を1ラウンドで解消 |
| メタ | コスト/効果 | core 2行 + app 5行・テスト235→237 |

結論2往復で収束。書き出しの言語整合性と冪等性を確保した。

## ソクラテス問答記録（2026-06-13・プリセットカード aria-pressed + WCAG 3.1.1 確認）

| 種別 | 問い | 結論 → 対応 |
|------|------|------------|
| 明確化 | 選択中のプリセットカードを SR はどう読み上げるか？ | `.selected` CSS クラスは視覚的のみ。ARIA 状態がなく SR は「ボタン」とのみ読む |
| 前提検証 | WCAG 4.1.2 は何を要求するか？ | UI コンポーネントの状態（選択・展開等）を名前・役割・値で SR に伝えること |
| 根拠 | 複数カードの中で1つが選択状態を持つパターンに適切な ARIA は？ | `aria-pressed="true/false"` が toggle button パターンとして最適。`aria-selected` は ARIA tablist や listbox 専用 |
| 視点転換 | WCAG 3.1.1（`<html lang>`）は実装済みか？テストで担保されているか？ | `applyLang()` 内で `document.documentElement.lang = lang` を設定済みだが、テストが存在しなかった |
| 帰結 | (1) preset card に `'aria-pressed':String(activePresetId===pre.id)` を追加 (2) WCAG 3.1.1 のテストを追加 | WCAG 4.1.2 と 3.1.1 を同時に担保 |
| メタ | コスト/効果 | app 1行変更・テスト237→239 |

結論2往復で収束。プリセット選択状態が SR に伝わるようになり WCAG 4.1.2 を充足した。

## ソクラテス問答記録（2026-06-13・統計テーブルの `<th scope="row">` 修正）

| 種別 | 問い | 結論 → 対応 |
|------|------|------------|
| 明確化 | 統計テーブル（三角形数・ランク等）の行ラベルは `<td>` か `<th>` か？ | 全て `<td>`。行ラベルと値の関係が HTML セマンティクスで表現されていない |
| 前提検証 | WCAG 1.3.1 はこのケースに適用されるか？ | Yes。データテーブルの行ヘッダーは `<th scope="row">` で表現する必要がある |
| 根拠 | `<th>` に変更すると CSS `.statTable td:first-child` が壊れるか？ | Yes。`td:first-child` セレクタは `th` に適用されない。CSS の更新が必要 |
| 視点転換 | 変更後の CSS はどう書くか？ | `th` と `td` で色・配置を分離: `th{color:var(--text-dim);text-align:left}`, `td{text-align:right}` |
| 帰結 | `row()` と PC/Quest 行を `el('th',{scope:'row'})` に変更し CSS を更新 | セマンティクスと視覚表現の両方を修正 |
| メタ | コスト/効果 | JS 3行 + CSS 3行変更・テスト239→240 |

結論2往復で収束。統計テーブルが適切な HTML セマンティクスを持ち WCAG 1.3.1 を充足した。

## ソクラテス問答記録（2026-06-13・JSON ファイルのドラッグ＆ドロップ読込）

| 種別 | 問い | 結論 → 対応 |
|------|------|------------|
| 明確化 | JSON パラメータファイルを読み込む唯一の方法は「読み込む」ボタンのクリックか？ | Yes。ドラッグ＆ドロップには対応していなかった |
| 前提検証 | 3D プレビューの canvas はマウスドラッグで回転する。衝突しないか？ | `e.dataTransfer.types.includes('Files')` で「ファイルドロップ」と「DOM 要素ドラッグ」を区別できる |
| 根拠 | `document.body` をターゲットにすればページ全体でドロップを受け付けられるか？ | Yes。`dragover.preventDefault()` で drop を有効化し、`drop` ハンドラで処理 |
| 視点転換 | JSON 以外のファイル（例: .vrm, .png）をドロップした場合の挙動は？ | ファイル名末尾と MIME タイプを確認。不一致は `err.loadFailed` で通知 |
| 帰結 | `dragover` + `drop` を `document.body` に追加し既存の deserialize ロジックを再利用。不使用 `.gacha-seed` CSS も削除 | ユーザー体験を向上しデッドコードを除去 |
| メタ | コスト/効果 | app 10行追加・CSS 1行削除・テスト240→242 |

結論2往復で収束。ドラッグ＆ドロップでの JSON 読込が可能になりファイル操作の摩擦を低減した。

## ソクラテス問答記録（2026-06-13・ブラウザタブタイトルの即時更新）

| 種別 | 問い | 結論 → 対応 |
|------|------|------------|
| 明確化 | JSON 読込後にブラウザタブタイトルはいつ更新されるか？ | `updateFnPrev()` が Export タブの `oninput` ハンドラ内にあるため、Export タブを開くまで更新されない |
| 前提検証 | プリセット選択やガチャ後も同様の問題があるか？ | Yes。`activePresetId` や `meta.title` が変わっても `fnameStem()` の結果がタイトルに反映されるのは Export タブ訪問時のみ |
| 根拠 | `fnameStem()` はグローバル関数で `rebuild()` から参照できるか？ | Yes。クロージャではなく module レベルの関数 |
| 視点転換 | `rebuild()` にタイトル更新を追加すると二重更新になるか？ | `updateFnPrev()` でも更新するが、後勝ちで同じ値になるため問題なし |
| 帰結 | `rebuild()` に `document.title='雛 — '+fnameStem()` を追加 | アバター再ビルドのたびにタブタイトルが即時反映 |
| メタ | コスト/効果 | 1行追加・テスト242→243 |

結論2往復で収束。ブラウザタブタイトルが常にアバターの現在の名前を反映するようになった。

## ソクラテス問答記録（2026-06-13・Page Visibility API によるアニメーション一時停止）

| 種別 | 問い | 結論 → 対応 |
|------|------|------------|
| 明確化 | ブラウザタブが非表示の時、rAF ループは止まるか？ | 止まらない。モダンブラウザは背景タブで 1fps に throttle するが、CPU/GPU 処理は継続する |
| 前提検証 | VRChat ツールはモバイル（Quest ブラウザ）でも使われるか？ | Yes。Quest の内蔵ブラウザからアクセス可能。電池消費は重大な UX 問題 |
| 根拠 | `visibilitychange` + `document.hidden` は全モダンブラウザで対応しているか？ | Yes。HTML Living Standard で標準化済み |
| 視点転換 | rAF 自体を停止する（`cancelAnimationFrame`）か、フラグで `renderFrame` をスキップするか？ | フラグの方が実装がシンプル。rAF 自体は継続するが renderFrame はスキップ。再開も即時 |
| 帰結 | `_rafPaused` フラグ + `visibilitychange` リスナー + ループ内条件チェック | 3行追加・テスト243→244 |

結論2往復で収束。タブ非表示中の CPU/GPU 消費をゼロにし、モバイルの電池寿命を改善した。

## ソクラテス問答記録（2026-06-13・ガチャシード 0 の再現性修正）

| 種別 | 問い | 結論 → 対応 |
|------|------|------------|
| 明確化 | `Math.random()*1e9|0` でシード 0 が生成された場合、シード入力欄で再実行できるか？ | できない。`if (Number.isFinite(n)&&n>0)` がシード 0 を除外する（falsy ではなく `>0` が問題） |
| 前提検証 | `mulberry32(0)` は有効な疑似乱数を生成するか？ | Yes。`a=0>>>0=0; a=0+0x6D2B79F5=1834746869` → 最初の呼び出しから有効な値を返す |
| 根拠 | シード 0 は除外すべき特殊値か？ | 否。シード 0 は他のシードと同様の疑似乱数列を生成する有効なシード |
| 視点転換 | 負のシードも `n>=0` で除外されるか？ | Yes。`Math.round(Number(input))` が負の場合、`n>=0` は false になり実行しない |
| 帰結 | `n>0` → `n>=0` に変更 | 1文字修正。テスト244→246 |

結論1往復で収束。シード 0 を含む全ての非負整数シードが再現可能になった。

## ソクラテス問答記録（2026-06-13・selfTest UV 範囲・法線正規化チェック追加）

| 種別 | 問い | 結論 → 対応 |
|------|------|------------|
| 明確化 | selfTest はメッシュの UV 座標が正常範囲（[0,1]）内かチェックするか？ | チェックしない。UV 逸脱はテクスチャ境界外サンプリングを引き起こし Unity で陰影破綻の原因になる |
| 前提検証 | 法線ベクトルの正規化はチェックされているか？ | チェックしない。非単位長の法線はトゥーンシェーディングで陰影方向がずれる |
| 根拠 | これらの不変条件は将来のコード変更で壊れうるか？ | UV 計算や法線生成ロジックの修正時に壊れる可能性がある。早期検出が重要 |
| 視点転換 | 実装コストは？ | 各5行。既存 `T()` フレームワークに追加するだけ |
| 帰結 | `UV coordinates in [0,1]` と `normals are unit length` の2チェックを追加。selfTest 20→22件 | |
| メタ | コスト/効果 | core 12行追加・selfTest 閾値更新 |

結論2往復で収束。メッシュ品質の核心となる UV と法線の不変条件が自動検証された。

## ソクラテス問答記録（2026-06-13・WebGL コンテキスト回復時の自動リロード）

| 種別 | 問い | 結論 → 対応 |
|------|------|------------|
| 明確化 | `webglcontextlost` で `preventDefault()` しているのに `webglcontextrestored` ハンドラがない。コンテキスト回復後はどうなるか？ | 何もしない。GL バッファ・テクスチャが無効のまま `renderFrame` は `GLOK` が true なので試みるが表示は壊れる |
| 前提検証 | コンテキスト回復後に完全再初期化（シェーダ・バッファ・テクスチャの再アップロード）は必要か？ | Yes。全 GL リソースが消えるため再アップロードが必要 |
| 根拠 | 完全再初期化の実装コストは？ | 高い。`initGL()`, `uploadGeometry()`, `uploadTexture()` を再実行し、シェーダも再コンパイルが必要 |
| 視点転換 | 最小コストの代替は？ | `location.reload()` でページ全体を再読み込み。localStorage に状態が保存されているため状態は維持される |
| 帰結 | `webglcontextrestored` → `location.reload()` で即時対応 | 1行追加・テスト246→247 |

結論2往復で収束。WebGL コンテキスト回復後もアプリが使用可能な状態を維持するようになった。

## ソクラテス問答記録（2026-06-13・activePresetId 復元時の有効性検証）

| 種別 | 問い | 結論 → 対応 |
|------|------|------------|
| 明確化 | 将来プリセット名が変更・削除された場合、localStorage に保存された古い `activePresetId` はどう扱われるか？ | 復元されるため、プリセットカードがハイライトされず、ファイル名が `hina_obsolete.vrm` になる |
| 前提検証 | 無効 ID が復元された場合の実害は？ | ファイル名が不正。プリセット選択 UI が整合しない（どのカードもハイライトされない） |
| 根拠 | `HINA.PRESETS.some(p=>p.id===id)` でバリデーションが可能か？ | Yes。`PRESETS` 配列はコアに公開済み |
| 視点転換 | 無効 ID を復元しない場合の挙動は？ | `activePresetId=null` のまま → `hina_custom.vrm` として扱われる（正しい） |
| 帰結 | `loadState()` で `HINA.PRESETS.some()` チェックを追加 | 1行変更・テスト247→248 |

結論2往復で収束。プリセット ID の後方互換性問題を予防的に解消した。

## ソクラテス問答記録（2026-06-13・カメラ距離の身長比例追従）

| 種別 | 問い | 結論 → 対応 |
|------|------|------------|
| 明確化 | プリセット切替で身長が変わると、カメラの見え方は適切に変化するか？ | `camTarget` は `uploadGeometry()` で更新されるが、`camDist` は起動時のみ設定。背の低いアバターに切り替えると相対的に遠く見える |
| 前提検証 | dblclick リセットは `build.dims.H` を参照するか？ | Yes。`camDist=(build?build.dims.H:1.45)*1.85` — リセットは正しい距離になる |
| 根拠 | `camDist` を毎rebuild で上書きするとユーザーのズームが失われないか？ | 比例スケーリング（`camDist * H / prevH`）なら、ユーザーが近づけていた場合もその距離比率を維持できる |
| 視点転換 | `prevH` はどこから計算するか？ | `camTarget[1] / 0.55` — `camTarget` が更新される前の値から逆算可能 |
| 帰結 | `uploadGeometry()` 内で `prevH` を計算し `camDist` を比例調整 | 3行追加・テスト248→249 |

結論2往復で収束。身長の異なるプリセット間でもカメラのフレーミングが一定に保たれるようになった。

## ソクラテス問答記録（2026-06-13・ドラッグ中の視線更新停止 + 視線角度クランプ）

| 種別 | 問い | 結論 → 対応 |
|------|------|------------|
| 明確化 | カメラを回転するためにドラッグしている間、アバターの目はどう動くか？ | `pointermove` ハンドラが常に `gazeX`/`gazeY` を更新するため、カメラ回転中も目がカーソルを追う。操作対象が「カメラ」なのに目が動くのは視覚的に不自然 |
| 前提検証 | `setPointerCapture` によりドラッグ中はキャンバス外でも `pointermove` が発火するか？ | Yes。ポインターキャプチャにより、カーソルがキャンバス外に出ても `pointermove` は canvas 宛に発火し続ける |
| 根拠 | キャンバス外でのドラッグで `gazeX`/`gazeY` はどこまで外れるか？ | `((clientX - left) / width - 0.5) * 2` はキャンバス幅と同距離外れると ±3 になる。`gazeX*0.22` で最大 ±0.66 rad（±38°）と不自然な角度になる |
| 視点転換 | 修正アプローチは「クランプのみ」か「ドラッグ中はスキップ」か？ | 両方を組み合わせる。ドラッグ中はスキップ（目がカメラ操作中に動かない）＋非ドラッグ時も [-1,1] にクランプ（外部からの event 等への防御）|
| 帰結 | `if (!drag){ gazeX=M.clamp(...,-1,1); gazeY=M.clamp(...,-1,1); return; }` に変更 | 3行変更・テスト249→250 |

結論2往復で収束。カメラ回転中の視線ちらつきを解消し、キャンバス外ドラッグによる過大な目の回転を防止した。
