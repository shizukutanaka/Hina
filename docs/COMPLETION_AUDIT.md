# 雛 (Hina) — 完成監査（イーロン・マスクのアルゴリズム適用・Round 526）

イーロン・マスクの5段階アルゴリズム（①要件を疑う ②部品/工程を削除 ③簡素化 ④サイクル加速 ⑤自動化、順序厳守）＋第一原理思考を雛に適用した記録。雛の非交渉制約（CLAUDE.md）の核心は「**単一HTML・依存ゼロ＝配布と監査の容易性**」であり、マスク法②「最良の部品は"部品なし"」はこの製品の存在理由に直結する。

## ① 要件を疑う（Question every requirement）

「要件は部門でなく個人から来る」の観点で、shipped されているものが本当に必要かを検査した。

- **i18nキー228個**: 全て参照されているか実測。→ **0個が dead**（後述）。要件は正当。
- **core の公開 export 41個**: 外部（app・tests）から本当に呼ばれるか実測。→ 2個が「防御的に export したが誰も使わない」＝**要件が個人でなく"習慣"由来**だった。削除対象（②へ）。
- **VRM 1.0 対応 / 指ボーン / プリセット追加**: FEATURE_AUDIT §3/§4 で既に「今は不要 or 視覚検証待ち」と判定済み。第一原理（3D非専門者をVRChatアバターまで最短で運ぶ）に照らし、v0.1スコープは妥当。

## ② 削除（Delete any part you can）

「10%を後で戻さないなら削り足りない」の精神で削除候補を実測。**ただし削除前に全動的参照を検証**（naive削除の罠を回避）。

| 対象 | 候補数 | 真に削除可 | 結果 |
|------|--------|-----------|------|
| i18nキー | 41（naive検出） | **0** | 全て `t(prefix+'.'+value)` 等の**動的参照**（`license.*`/`allowed.*`/`usage.*` は `selRow` の `t(tp+'.'+o)`、`guide.*` は配列ループ）。naive削除は許可/ライセンスUIを破壊していた |
| CSSルール | 3（`.row-less`等） | **0** | いずれも Round 497/498 の履歴を説明する**コメント内言及**で、実ルールではない。コメントは文脈として価値があり保持 |
| core export | 2（`SKIRT_OUTFITS`/`buildSkeleton`） | **2** | 内部使用のみ（`hasSkirt()`・`buildAvatar()` が内部で使用）で外部参照ゼロ。**定義は残し export のみ削除**（41→39）。公開面が縮小し監査負荷が減る |
| 共有状態 `px`/`py`（Round 528） | 2変数 | **2** | ポインタハンドラの共有アンカーが**最後の既知欠陥の原因そのもの**だった（2本目の指がアンカーを汚染→離すと視点が1.504rad跳ぶ、CDP実タッチで再現）。既存の `ptrs` Map が per-pointer で正しい位置を保持しているため、**共有変数を削除すると欠陥も消えた**。マスク法②の教科書例 |

**教訓（第一原理）**: 削除の前に「本当に未使用か」を実測で証明せよ。雛は動的参照（文字列連結でキー構築）が多く、`grep 't(\'key\')'` だけでは live なキーを dead と誤判定する。この罠を3回回避した（i18n・CSS・exportの各検査で正確な出現カウントに切り替えた）。

## ③ 簡素化（Simplify）— 削除の後にのみ

削除で1件の副作用（export削除でreturnブロックが伸び、正規表現ウィンドウ型テストの400字境界を越えて失敗）が出たため、説明コメントを`return {`の外へ移してブロックをコンパクトに保った。これは SWOT #4（正規表現ウィンドウ型テストの脆さ）の実例であり、テストを弱めず構造で解消。他に安全に簡素化できる過剰は検出されず＝**既に最小に近い**。

## ④ サイクル加速 / ⑤ 自動化

- **ビルド**: `cat build/* > index.html` の1コマンド。加速の余地なし。
- **テスト**: `node tests/run.js` が **2045件を約2秒**。十分高速。
- **検証の自動化**（Round 520）: `tools/render-check.js` が視覚回帰を自動判定。仕様適合（Round 515スイート）＋公式バリデータ（Round 524）＋参照ローダー three-vrm（Round 525）で多層自動化済み。
- **残る手動工程**: リポジトリ公開設定（default branch / Release / GitHub Pages）＝Settings権限が必要で**人間ゲート**。ここは自動化不能（SWOT #6）。

## 完成の評価（第一原理）

「完成」を「3D非専門者が、インストール不要・データを預けず・パフォーマンスゲートに阻まれず、VRChatアバターを手にできる」と定義すると:

- 生成・プレビュー・書き出し・導入ガイド（アプリ内自己完結・Round 512）・ランク保証・ワンタップ修正（Round 513）まで**すべて実装済み**。
- 出力は**公式 glTF-Validator で0 errors**、**参照ローダー three-vrm で完全読込**。
- コードは**実測で最小**（dead i18n/CSS/export ゼロ、export面を縮小）。

→ **プロダクトとしては完成**。残るのは人間ゲートの公開設定3点（各1–2クリック、SWOT改善案#1）のみで、これはコードの問題ではなく権限の問題。

### 既知バグゼロの達成（Round 528）

Round 528 で FEATURE_AUDIT §3-4 の最後の未修正欠陥（マルチタッチ視点ジャンプ）を修正し、**雛自身の既知の未修正欠陥はゼロ**になった。§3-4 に残る blink 加算重複の項目は「雛の管轄外」と一次仕様照合で結論済み（雛アプリに同時適用経路がなく、書き出す定義自体は正しい）で、欠陥ではない。

完成の全層エビデンス:

| 層 | 状態 |
|---|---|
| 自動テスト | **2053件 / 0失敗** |
| 公式 Khronos glTF-Validator | **全11書き出し 0 errors** |
| 参照ローダー three-vrm | **全ケース完全読込**（humanoid15・表情17プリセット意味的マッピング・springBone・meta往復） |
| 視覚回帰 render-check | **全13ケース ok** |
| 書き出しE2E（実ブラウザ） | **ボタン→DL→公式バリデータ0 errors**（Round 529。両保存経路・キャンセル復帰も検証） |
| 自己診断 `?selftest`（実ブラウザ） | **22/22 all OK**（Round 530） |
| 自動保存の往復（実ブラウザ） | **UI編集→リロード→復元を確認**（Round 530。保存経路は2系統で冗長） |
| 共有URL `?seed=N`（実ブラウザ） | **同一シード再現＋別シード相違の対で検証**（Round 531） |
| Undo/Redo（実キーボード） | **Ctrl+Z/Ctrl+Shift+Z 往復成立**（Round 531） |
| 既知の未修正欠陥 | **ゼロ** |
| dead code（i18n/CSS/export） | **ゼロ** |

## ソクラテス問答による完成主張の検証（Round 529–531）

マスク法①（要件を疑う）を**自分の完成宣言そのもの**へ適用する形で、ソクラテス式の問いを繰り返した。各問いは仮定を暴き、いくつかは実際の穴を発見した。

| 問い | 暴かれた仮定 | 帰結 |
|------|------------|------|
| 「書き出しは検証済みか？」 | 検証は全て `exportVRM()` のNode呼び出しで、**ユーザーが押すボタンは未検証**だった | Round 529: E2E実施→DLファイルが公式バリデータ0 errors。恒久化 |
| 「タグ付けは本当に不可能か？」 | 「人間ゲート」は仮定だった | 実測: タグpushはHTTP 403→仮定が**検証済みの事実**へ昇格 |
| 「自己診断は動くか？」 | `?selftest` は一度もブラウザで開かれていなかった | Round 530: 22/22 all OK |
| 「自動保存は本当に守るか？」 | ユニットテストのみでリロード往復は未検証 | Round 530: UI編集→リロード→完全復元。**変異で保存経路が2系統ある冗長性も発見** |
| 「共有URLは約束を果たすか？」 | 決定論だけ検査すると「シードを無視して常に同じ結果」でも通る | Round 531: 同一シード再現**かつ**別シード相違の対で検証→両方成立 |
| 「Undo/Redoは実キーボードで動くか？」 | 関数呼び出しテストのみだった | Round 531: 実`Ctrl+Z`/`Ctrl+Shift+Z`で往復成立 |
| 「セキュリティ硬化を長所に掲げているが、**実際に攻撃したことはあるか**？」 | prototype pollution 対策は `sanitize*` の**単体テストのみ**で、悪意あるJSONを実ブラウザの実際の読込経路へ流し込んだことは一度も無かった | Round 535: 4経路（ファイル入力/D&D/汚染localStorage/3MB巨大ファイル）へ実際に攻撃 → **汚染ゼロ・不正enumは既定値へ・巨大ファイルは拒否**。ただし**5,000字のtitleが素通し**する不整合を発見し修正（下記） |
| 「攻撃はメタ情報だけ試したのでは? **最も無防備なのは書き出しファイル名**ではないか?」 | ファイル名は `meta.title` からOSへ直接渡るのに、パストラバーサルや区切り文字を試したことが無かった | Round 536: `safeName()`（全書き出しの単一関門）へ20種の攻撃を投入 → **パス脱出は不可**（区切り文字は`_`置換）と確認。ただし制御文字・BIDI上書き・先頭ドットの3欠落と、**Windows予約名ガードが拡張子付き（`CON.vrm`）を取り逃す実装漏れ**を発見し修正 |
| 「テストは2094件あると言うが、**そのうち何件が実際にコードを動かしているのか**? ソース文字列を正規表現で照合しているだけのテストは、リファクタで静かに空検証にならないか?」 | SWOT短所#4として自認済みでありながら「運用規約でカバー中」と書いていた。規約は制御ではなく、実際にR496/497・R526・R536と3度発生していた | Round 537: 窓アンカー57種の死活と、`html.slice()`由来の窓42個の番人（肯定アサーション）有無を機械で実測 → アンカーは全て健在、**番人不在の窓が3個**。うち`barFnBody`は**通読では発見できず機械のみが検出**。3件を修正のうえ、両規則を**スイート自身が検査するメタテスト**として恒久化し、短所#4を「規約」から「テスト」へ格上げ |
| 「『この環境から実行可能な改善は無い』という Round 533 の宣言は、**534/535/536/537と4回連続で反証**されている。ならば誤っているのは環境ではなく**判定基準**ではないか? 何が"実行不可能"の判定を誤らせたのか?」 | SWOT短所#1は「色・テクスチャは検証不能」と書いていた。だがその根拠は**SwiftShaderのcanvas→テクスチャ経路**という**プレビュー描画に固有の**制約であり、それを製品全体の制約へ一般化していた | Round 538: ユーザーがVRChatで見る色は**書き出された`.vrm`に埋め込まれたPNG**＝ファイルであってGPU不要と気づき、GLB→PNG→zlibデコードで実ピクセルを検証。**「不可能」は誤りだった**。しかもこの誤りにより色パイプラインは**E2E被覆ゼロ**で、`block('skin', p.skinTone)`→`p.hairColor`の変異（全アバターの肌が誤色＝VRChatで即座に見える不具合）が**2094件のテスト・公式バリデータ・three-vrmを全て素通り**した |
| 「色がアトラスに正しく載ることは Round 538 で示した。だが**メッシュのどの部分がどのブロックを参照しているか**は誰も検証していないのではないか?」 | UVが `[0,1]` に入ることと、アトラス矩形が重ならないことは検証済みだった。しかし「この頂点はどのブロックを見るか」という**対応付け**は一度も検査されていなかった | Round 539: 全頂点UVをアトラス領域へ分類 → `clothSub` を参照する三角形は `shirts` にしか存在しないと判明。既定の `onepiece` を含む3衣装で「服サブ」ピッカーが**沈黙して何もしないコントロール**になっていた。非表示化し、判定を core に一本化 |
| 「服サブは効かないコントロールだった。**ならば他のパラメータはどうか?** 全PARAMSのうち、実際に出力を変えるものは何件か?」 | Round 539 は1件の不具合を直したが、**同じ種類が他にもあるか**は確かめていなかった。個別に狩り続けるのは方法として弱い | Round 540: 全38パラメータを摂動し、ジオメトリか書き出しバイト列が動くかを実測 → **動かないのは11個のみで全てアトラス側が消費するもの**、沈黙するコントロールは他に無しと確認。この集合を**双方向の集合等価**で恒久固定し、アトラス側11個はシード間のテクスチャ領域比較で担保した |
| 「**UIが明示的に約束している**コントロールはどれか。その約束は、ユーザーが実際に到達できる状態すべてで果たされるか?」 | 「Quest Excellentにする」ボタンの根拠は、コード内コメントの「6プリセット＋300ガチャロールで測定」だけだった。だが**プリセットもガチャも、スライダで到達できる空間を代表しない** | Round 541: 全パラメータ独立ランダムの20,000状態で実測 → **約束は果たされていた**（反例ゼロ）。ただし根拠がコメントのみでテストが無く、将来の重い機能でボタンが黙って嘘をつきうる状態だったため、3,000状態の決定論的掃引として恒久化 |
| 「SWOTが長所として挙げる『**完全キーボード操作**』は、実際にキーを押して確かめたことがあるのか?」 | roving tabindex（`tabindex="-1"`）を使うグループは、**矢印キーのハンドラが動いている場合のみ**正しい実装であり、ハンドラが無ければ同じマークアップのまま「キーボードから到達不能」になる。**静的監査では両者を区別できない** | Round 542: 4グループすべてで実キー入力により検証 → **主張は正しかった**（Arrow/Home/End/Enter/Space すべて動作）。`tools/render-check.js` に恒久化。**自己訂正3件**: Home・Enter・End が効かないと誤読したが、**3件とも測定側の誤り**（起点が既に端にあった／再描画でハンドルが失効／roving グループの境界を54個全体と取り違えた）。製品の不具合は1件も無く、いずれも報告前に実測で反証した |
| 「最も重い約束は『通信ゼロ・オフライン保証』（CLAUDE.mdが交渉不可とする根拠そのもの）だ。**これは何によって担保されているのか?**」 | 担保はソース正規表現3本だけだった。正規表現は**実行時に組み立てたURL**（`fetch(変数)`）を見られず、さらに **XHR・WebSocket・sendBeacon・EventSource・動的`import()`・Service Worker はどのテストにも無かった**——SWOTは「担保されている」と書いていたが**それは事実ではなかった** | Round 543: 不足分をソーステスト化し、加えて**実ブラウザのリクエストログ＋5種APIのトラップ**でアプリ全体を操作する監査を追加。実測結果は**リクエスト1件（文書自身）のみ・API呼び出しゼロ**。「何も起きない」検査が空回りしないよう、タブ切替・ガチャの実変化・書き出しファイル生成を**実行された証拠として必須化**した |
| 「CLAUDE.md は `docs/SPEC.md` を『実装と乖離したらSPECが正』と定めている。**では、自分が Round 539 で挙動を変えたとき SPEC を直したか?**」 | 直していなかった。SPEC は Round 533 以降未更新で、F-007 は「服メイン/サブ/アクセント」を**無条件**と記していた。プロジェクト自身の規則では、これは放置していい不整合ではなく**不具合**である | Round 544: F-007 を日英両方で更新し、さらに**SPECとコードの整合をテスト化**——機能表IDの日英パリティ、記載数値（プリセット6・髪型5・肌パレット8）とコードの一致、`usesClothSub()` の記述。変異テスト3件（SPECを539前の記述へ戻す／英語側のF行を削除／数値を古くする）すべてで検出を確認 |
| 「『dead code ゼロ』は Round 526 の**手作業**の測定だ。18ラウンド分の変更を経た今も本当か?」 | 素朴に「リテラル参照が無いキー」を数えると228中73件が未参照に見える。だが**その大半は実行時に組み立てられる**（`t('tab.'+id)` / `t(tp+'.'+value)` など）——素朴な検出器は狼少年になり、やがて無視される | Round 545: 各動的ファミリを**それを駆動する実データ**（PARAMS.opts / META_ENUMS / RANKS / morphs / TABS）へ解決する検出器を実装。結果は**孤児ゼロ・公開シンボルの死蔵ゼロで主張は維持**。加えて逆方向（存在する選択肢に日英ラベルがあるか）も検査。**自己訂正2件**: `tab.*` と `expr.blink_l/blink_r` を孤児と誤検出したが、いずれも検出器側の誤り（TABS配列を読んでいなかった／表情バーが per-eye blink を意図的に除外している）|
| 「Firefox / Safari で動くか？」（SPEC §7 は4ブラウザ対応を明記） | E2Eは**全てChromium**（SwiftShader）上だった | Round 534: Firefox/WebKit の導入を試行→CDNダウンロードがプロキシに遮断され**実行検証は不可**。代替として全ブラウザ固有APIの**静的ガード監査**を実施し全件✅（`showSaveFilePicker`/`share`/`clipboard`/WebGL/`dvh`/`inert` は全て機能検出または優雅な劣化。`structuredClone` のみ無ガードだが Firefox 94+/Safari 15.4+ で仕様§7の範囲内）。「動くはず」と「動くことを確認した」を区別し、後者は**残る未知**へ |

**問いが暴けなかったもの（残る未知）**: Unity/UniVRM実機での取込・色/テクスチャの見た目・VRChat内での実挙動・**Firefox/Safari での実行**（静的監査のみ、Round 534）。これらはこの環境の原理的限界（SWOT #1/#2）で、**問いを立てたうえで「答えられない」と記録すること**が誠実な回答である。

## 後続セッションへ

このアルゴリズムを再適用する際は、②の削除ハントで**必ず動的参照を実測**すること（`t(prefix+`）。naive な `t('literal')` grep は live キーを dead と誤判定する。
